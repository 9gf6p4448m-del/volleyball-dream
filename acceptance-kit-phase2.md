# 配色卷階段二 驗收條件凍結（P1–P7）— 2026-08-24

> 凍結時基準 SHA：518c018（session 起點，工作樹乾淨）。
> 權威依據＝`docs/kickoffs/team-kit-phase2-ruling.md`（Claude.ai 六題拍板凍結稿）；
> 本檔為其 §七 驗收清單的凍結落點，內容不改寫、僅編號化。任何修改走 02 §2.1。

- **P1 sim-hash**：逐值同基準 `34772c06e02243fd`
- **P2 大學章實開一場**（瀏覽器，非 node）：我方穿入學校 jersey 色；對手照舊；必含我方 libero 在場畫面截圖（小守後排輪次入鏡）——「資料有設」與「畫面有穿」分開驗
- **P3 高中章＋快速比賽＋練習賽**：藍/金與預設逐值不動（git diff＋瀏覽器實開對照）
- **P4 色距斷言**：按 ruling §三量測結果裁定後升級 `checkKitPalette()`，進 node 永久守門
- **P5 隊名 read-back**（大學章畫面）：隊名顯示＝入學校名；「遊隼高中」殘留處逐一截圖列出並標明排除類別（高中歷史區／故事文本），不得只回報「已通過」
- **P6 「進行中」雙向**：高中第 3 屆完結後不再標示；真正進行中的屆照標
- **P7 瀏覽器實開為必要條件**（前車之雷：kits 組裝層 node 零覆蓋，ReferenceError 活 10 天）

停報條件 T0/T1/T2＋通則、do-not-touch 五項、施工順序：一律照 ruling §四／§五／§八，本檔不重抄。

---

## v2 回填（2026-08-24 重裁拍板稿，使用者裁定；權威＝`docs/kickoffs/team-kit-phase2-ruling-v2.md`）

**P4 修訂紀錄（02 §2.1 使用者明確裁定路徑）**：原 P4「色距斷言按量測結果升級」——量測落大面積不足分支，使用者重裁走 B 案：色距硬門檻只剩 identity（`IDENTITY_MIN`=100，現況 36 對全過），`READABILITY_MIN`=150 檢查降級為資訊性輸出、常數與函式本體保留不刪。原 ruling §三第二分支的「8 對候選色值逐項核可」隨 B 案生效而失效。

**回填比對紀錄**：v2「回填事項」要求對照凍結稿 B 案定義——凍結稿（ruling.md:53）僅「B 案（UI 語意通道）」一句、無通道清單與驗收寫法之凍結定義；開卷稿（kickoff.md:42）「接受 ≥100，敵我判讀靠 UI 語意色通道」與 v2 一致。無出入，免回修。

**永久註記（v2 裁定一之 4，V7）**：北陵大學 × 海硯大學＝102.44，距 identity 底線僅 2.44——**薄餘裕對**。未來任何一校色票變動時，此對必須重量測。

### v2 驗收清單 V1–V7（凍結；與 P1–P7 並行有效，P4 以本節修訂為準）

- **V1**：`npm test` 全過；identity 檢查（100）恆過
- **V2**：高中章 7 處隊名（對陣畫面／主畫面戰績列／名次板／隊友卡片／情蒐行／球場看板／戰術板）全顯「遊隼高中」（行為不變）
- **V3**：大學章（devSeed 或真實升學路徑）上述 7 處全顯所選大學校名，零處殘留「遊隼」
- **V4**：`grep -rn "遊隼" src/` 命中只剩排除清單（events.js／rivalArc.js／careerFinale.js／roster.js／graduation.js 註解／n2Arc.js:65／positionEvents.js:151／careerScreen.js:2259）
- **V5**：careerScreen.js:2259 帶 `// TODO(uni-finale)` 標籤
- **V6**：歷屆清單：archive 內各屆不標「進行中」；即時季收束後（champion/eliminated）不再標；高中第 3 屆完結後不標
- **V7**：凍結稿含北陵×海硯薄餘裕對永久註記（＝本節上方）

停報條件 S1–S4＋通則照 v2 稿；施工順序照原 ruling §八（量測關卡已由 v2 裁定關閉，T2 已通過）。

---

## 施工期修訂紀錄（2026-08-25，兩項皆使用者於對話中明確裁定）

1. **隊友卡片排除（V2/V3 的 7 處改 6 處）**：凍結稿把生涯數據頁隊友卡片（careerScreen.js:2619 `${OUR_TEAM_NAME}・那三年的隊友`）列為第 7 個動態隊名消費端，實作時發現其渲染的是升學時封存的高中名冊（`loadHighSchoolRoster`，careerStore.js:369）、只在大學章出現、語意＝高中歷史——掛動態隊名會把大學校名印在高中歷史卡上。**原標準錯在**：把「畫面上出現我方隊名」全數視為「指涉現任隊伍」，漏判此卡指涉封存歷史；**現在才知道**因凍結稿產出時無此卡的渲染脈絡。使用者裁定：比照 careerScreen.js:2259 排除、維持靜態 `OUR_TEAM_NAME`。V2/V3 的「7 處」據此改為 6 處（對陣抬頭／戰績列／名次板／情蒐行／球場看板／戰術板），排除清單增列此處。
2. **E5 改消費端章節感知**：v2 裁定三指定即時季用 `careerStage()` 判收束，對抗覆審實證 `careerStage()` 只認高中賽程 schema、對大學章恆回 `'national'`（打完 8 場的大學球季恆標「進行中」）。**原標準錯在**：「careerStage＝章節脫鉤的收束事實」這個前提對大學章不成立；**現在才知道**因大學賽程 schema（`stage:'uni'`/`round:'league'`）是批 6 後才有、裁定時未實測。使用者裁定：消費端章節感知——高中章照 `careerStage`、大學章比照既有 `uniSeasonDone` 先例（league 全有結果＝收束），單一定義收斂在 `liveSeasonOngoing()`（careerScreen.js）。V6 語意不變、涵蓋面擴及大學章（加嚴），新增測試 E5④⑤守門。
