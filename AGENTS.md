# 排球夢：3D／美術／體驗決策

本檔補充 `CLAUDE.md`；既有 `docs/design-brief.md` 的已定案決策不重開討論。涉及玩家可見的 3D 視覺、美術風格或核心遊戲體驗時，預設由 `gpt-6-astra` 主責，並以高推理強度處理。範圍包括：場景構圖、鏡頭、夜賽燈光、幾何關節球員的比例與辨識、材質、後製、動作節奏、UI 與 3D 的資訊層級、比賽回饋與跨系統視覺架構。

## Astra 主責流程

1. 先檢視現況、已有規格、可重現證據與玩家回饋。
2. 若存在實質取捨，提出可選方向、預期體驗與代價；若只有單一低風險修正，說明依據後直接執行。
3. Astra 裁定視覺方向、架構邊界與驗收標準，並負責最終的玩家可讀性與視覺驗收。

可將低風險、邊界明確的工作交由其他模型：資產與程式碼蒐證、例行測試、固定規格下的機械修改與重現步驟。這些結果必須回交 Astra；其他模型不得單獨推翻 Astra 的視覺或架構決定。

下列工作仍依全域模型矩陣處理，無須因為是遊戲而升級 Astra：格式整理、資產命名與盤點、既有規格明確的微調、單一且成因已知的 bug 修復、例行測試。

使用者明確指定的模型、流程或設計決策優先於本檔；排球模擬核心仍必須遵守 `CLAUDE.md` 的決定論、固定步長與零 three.js 依賴鐵律；安全、資料完整性與其他更高優先規則仍照其原規則處理。
