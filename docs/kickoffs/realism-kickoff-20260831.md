# 真實感卷 kickoff（2026-08-31）

Sawmah 就「整個遊戲真實感還有什麼可以做」拍板「按照你的建議」：
批1 裁判可視化 → 批2 鷹眼貼線複審演出（表現層）→ 批3 觸網犯規 → 批4 強隊 commit 配比（sim）。
傷病系統明確跳過（傷爽度）。

> 提案更正（開卷勘查發現）：原提案稱「L 改判機制已存在、鷹眼只包皮」**有誤**——
> 「L 改判」是自由人推翻 AI 防守指令（W4 Q9），不是挑戰判決；本遊戲沒有判決挑戰機制。
> 批2 據此改為「貼線球鷹眼複審演出」：sim 判定即真值、永遠維持原判，演的是轉播儀式。

## 驗收凍結（動手前訂，改動照 02 §2.1；開卷基準 main f4aaa8a）

通用：批1/2 純表現層——`src/sim/**` 零改動、sim-hash 逐值同；批3/4 動 sim——
`--write --reason` 重立＋前後全表（tools/balance-recal-probe.mjs）；全套 npm test 綠，
既有測試紅走同意程序（同攻守平衡卷）；視覺與頻率由試玩裁定，數值標【試玩必調】；
每批部署附送達證明。

### 批1 裁判可視化
- R1-1 場邊主審（裁判台＋人形，網柱側）＋兩名司線員（對角線後角），三館皆有；
  幾何走程序化剪影（沿 arena 零模型檔路線），靜態成本近零
- R1-2 死球演出：得分＝主審手臂指向得分方；出界（OUT）＝落點側司線員舉旗；
  時序對齊既有哨音（sfx 死球長哨），不另加音效
- R1-3 純表現層：讀既有事件流（DEAD_BALL/SCORE），sim 與事件型別零改動
- R1-4 崩潰 try/catch 退回無裁判畫面（永不致死）；動畫只在死球窗跑（窗外零更新）

### 批2 鷹眼貼線複審演出
- R2-1 觸發：殺球/發球落地（BALL_IN 或 OUT）且落點離最近界線 ≤ 門檻
  （初值取 line-kill 分布的貼線帶，標【試玩必調】）→ 播鷹眼動畫：
  俯視落點放大＋球印橢圓＋IN/OUT 翻牌——結果恆＝sim 原判（不存在改判）
- R2-2 點擊可跳過；播畢/跳過回到現行流程，比分結果零改動；與重播/慶祝不打架
  （已在演出中則不觸發）
- R2-3 頻率節流：同一局至多 N 次（初值 2【試玩必調】）——轉播儀式不是每球都演
- R2-4 純表現層：sim 零改動

### 批3 觸網犯規（動 sim）
- R3-1 新犯規：攔網觸球（graze/body/press）後有機率判定觸網——死球、判給攻方，
  DEAD_BALL 帶新 reason `NET_FAULT`；字卡＋哨音走既有死球通道
- R3-2 機率模型（決定論 blownHash，零 rand 消耗）：press（壓手）觸網倍率 > vertical
  ——與「壓手的代價」設計一脈相承（手過網＝收手擦網風險）
- R3-3 頻率帶：全場實測 0.3–1.5 次/局【試玩必調】；攻守全表回歸：非攔網格 ±3pp 內，
  攔網相關格位移如實記錄
- R3-4 reason 消費端逐一巡過（字卡/播報/典藏/統計）：新 reason 不得讓任何消費端
  靜默壞掉或顯示內部代號
### 批4 強隊 commit 配比（動資料）
- R4-1 生涯強隊（全國賽後段種子/職業/海外上位）blockPersona 改 commit；名單開工時
  列出、試玩可調；快速比賽與弱隊維持 read（出廠語意不變）
- R4-2 配對同種子量測：改動隊伍對玩家勝率位移如實記錄（位移大＝回報拍板再上）
