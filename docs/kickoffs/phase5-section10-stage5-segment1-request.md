# Phase 5 §十 階段五 基準 B —— **段 1 回報＋四項送裁**

> **前情**：基準 A（球體遷移＋v4 手點）已綠燈；段 1（t=0.25，五旋鈕同步）**已完成量測**。
> **基準**：`main@56887a3`｜781 測／780 綠／**1 紅**（＝送裁項 ④）｜
> 段 1 hash **`05384976bf4dd961`**（基準 A 為 `06343a6e17904c4e`）
> **要裁：四項。** 其中 ① 決定我能不能判段 1 過關，② 決定 `poor` 要等到第幾段才反解得出來。
> **段 2 未開**，等裁定。

---

## 零、機制交代（先講清楚，因為它決定後面的數字怎麼讀）

- **`CONVERGE_T` 住在 `constants.js`**：`blockBand` 需要它算收斂後的攔網半寬，
  而 `blockBand` 不能 import `game.js`（後者反過來 import 前者 ⇒ 循環）。
- **內插空間（A-7 的精神，實作端必須交代的選擇）**：
  接／舉／扣的 **t=0 是絕對公尺（1.30）、t=1 是身高比例** ⇒ 兩端單位不同。
  **唯一兩端都有定義的空間是「絕對公尺、逐球員」**：`r(t) = (1−t)×1.30 + t×(比例×H)`。
  魚躍的 t=1 是**公尺絕對值**（v4 拍板 1）⇒ `r(t) = (1−t)×2.34 + t×2.0`
  ⇒ **`DIVE_REACH_MUL` 在 t=1 自然退場**（魚躍不再是乘法量）。
- **t=0 逐值重現基準 A**（hash `06343a6e17904c4e`、781 全綠）
  ＝這套機制沒有偷帶任何東西的證明。
- **攔網半寬維持單一真相**：收斂在 `blockBand.js` 算完，sim 的 `TUNING.BLOCK_REACH_X`
  與玩家面板 `input/attackZones.js` 吃**同一個值**。
  ⚠ 實作端一度把收斂值放進 `TUNING` 導致兩邊分岔，被 `reach-volume.test.mjs` 的
  「陷阱 1」測試抓到並修正——**那條測試存在就是為了抓這個**。

---

## 一、段 1 量測結果（面板七項全量）

| 項 | 基準 A（t=0） | **段 1（t=0.25）** |
|---|---|---|
| P1(a) **貼邊率** `d̂∈[0.9,1]` | 接 0.0／舉 0.0／扣 0.0／魚躍 **31.1%** | 接 **3.0**／舉 **14.0**／扣 1.5／魚躍 **37.0%** |
| P1(a) 可及上限（逐球員） | 接 1.405／舉 1.405／扣 1.405／魚躍 2.445 | 接 1.254–1.266／舉 1.286–1.294／扣 1.332–1.350／魚躍 2.360 |
| P1(a) n（10 局） | 951／955／984／**45** | 834／788／812／**27** |
| P1(b) 持球回合／rally／**rally 長度** | 1734／481／**490.3 tick** | 1475／468／**437.8 tick（−10.7%）** |
| P2 `passTier` | perfect 99.8／poor 0.2 | **perfect 100.0**（更退化） |
| P3 密合率 | 實際半寬 1.100 ⇒ **100%**；終點 0.5 ⇒ 94.1／快攻 85.6 | 實際半寬 **0.950** ⇒ **100%**；終點 0.5 ⇒ 93.9／快攻 84.5 |
| P4 E 表 G%（快／兩翼／後排） | read 55.1／98.1／84.4｜commit **68.0**／16.6／68.1 | read 57.9／97.5／67.4｜commit **61.1**／9.7／57.4 |
| P5 難度（**配對同種子**） | （基準） | **Δg1 −15.0±7.6**｜Δg2 −2.5±11.0｜Δg3 +7.5±11.5｜ΔQF +12.5±10.2｜ΔSF +17.5±10.7｜Δ決賽 −5.0±10.7｜**Δ奪冠 −2.5±6.7** |
| P7 `sim-hash` | `06343a6e17904c4e` | **`05384976bf4dd961`** |

**「預告」功能首次啟動**：舉球貼邊率 0.0 → **14.0%**、魚躍 31.1 → **37.0%**
——那就是 P1(a) 十等分最上格被設計出來要抓的東西。

---

## 二、要裁的四項

### ① 🔴 ③ 停手判準的**數字**尚未裁定 ⇒ 實作端無法判段 1 過不過

附錄 §4.3 判「③ **延後**，但形式現在裁死：**必須是配對同種子的差值**，不得用未配對絕對值」。

**形式已備妥並實跑**（`VD_PAIRED`，自驗差值全 0）。段 1 的配對結果見上表。
**但沒有數字 ⇒ 實作端無法判定段 1 是否通過，也無法決定要不要對半切（§4.2① 允許但要登記）。**

判讀材料：
- **唯一明確的是 `Δgroup-1 −15.0pp ± 7.6`（≈2 SE）** ——新人局變難 15 個百分點
  （註：基準 A 時 group-1 是 95%，段 1 拉回約 80%，反而接近圓柱時代的 78%）
- **`Δ奪冠率 −2.5pp ± 6.7`** ——在雜訊內
- 其餘六項皆 ≤1.6 SE

⇒ **請裁**：停手判準用哪個量、帶寬多少（例如「Δ奪冠率 超出 ±X pp 即停手」）。

### ② 🔴 C-1 的 `poor` 係數反解，在段 1 **數學上無解**

C 裁定：`poor` 目標佔比 **5%**、係數由量測反解、**`ok` 地板 8%**（C-2 跌破即停手回報）。

段 1 實測：d p10 0.054／p50 0.244／p90 0.441／**非爆接 max 0.556**；舉球可及 ≈ **1.18**。

- 要 `poor` ＝ 5% ⇒ `poor` 門檻須落在 d 的 95 分位 ≈ **0.48**
- 但 `perfect` 門檻 ＝ 0.923 × 1.18 ＝ **1.09**
- ⇒ **`poor` 門檻（0.48）會低於 `perfect` 門檻（1.09）**
  ⇒ 分檔不連貫（`perfect` 把一切吃掉），`ok` 恆 0%（實測正是 perfect 100.0%）

⇒ **在舉球可及縮到約 0.45–0.60 之前，`poor` ＝ 5% 無解。**
與階段四的反事實表完全一致（可及 1.30／0.80／0.60 三檔都是 99.9% perfect，
要到 **0.45** 才出現 `ok` 17.4%）。

依 C-2 的精神（「**不准實作端自己在懸崖上取捨**」）**回報，不自行挑係數**。

⇒ **請裁**：C-1「每段重解」在無解的段落怎麼辦——
（a）該段登記「無解」並跳過，到可及進入可解區間（約段 3–4）才開始反解
（b）改成「可解即解、不可解即登記」的條件式
（c）其他。

### ③ 🔴 魚躍 n=27 < 30 ⇒ 本段不判

v4 §2.3 明訂「**n < 30 即視為未完成，不得判定**」。
基準 A 時 n=45（自然對戰）已通過；段 1 掉到 **27**（−40%）。

⇒ 依 v4 條文，**魚躍這一格本段不判**。
⚠ 但同時魚躍的**貼邊率已 37.0%**（全動作最高）⇒ 它是收斂中最先受壓的動作，
**恰好也是樣本最少、最判不動的那一個**。

⇒ **請裁**：是否要求以 v4 §2.3 的「強制魚躍情境」（提高對方攻擊落點偏遠角比重，
或直接注入測試情境）把每段的魚躍樣本補到 n≥30。
⚠ 實作端提醒：注入情境會讓魚躍那一格的樣本**與其他六項不同源**（不同的對戰分佈），
依 **A-8**（比較同源檢查）那組數字不能與其他項並排比較，只能自己跟自己的段間比。

### ④ 🔴 `jump-set ⑤b` 治具前提破（唯一的紅）

三局合計 **442**（162+132+148）未達 **450**（＝每局 150 × 3）。

- **門檻 150/局 未動**（上一輪裁定：多 seed 合計、標準不調）
- 破的原因是 **rally 長度 −10.7%**（490.3 → 437.8 tick）＝**收斂的真實後果**，不是儀器問題
- 決定論本身沒壞：`⑤a`（`deepEqual`）綠、`block-persona ⑤` 綠、`sim-hash` 重跑逐值相同

⇒ **請裁**：（a）接受這條紅在整個基準 B 期間存在，收斂完成後重評
（b）再放寬 seed 數（例如四局合計 > 600）——但那是第三次調同一個治具
（c）把「rally 長度」正式列為觀測項，並讓 ⑤b 掛在它上面
（d）其他。

---

## 三、兩個觀察項（不構成停手，但要帶回）

1. **快攻的賭局 margin 正在收縮**：`commit G − read G` ＝
   基準 A **12.9pp**（68.0−55.1）→ 段 1 **3.2pp**（61.1−57.9）。
   R4 三款的**方向仍全部維持**，但快攻這一格**正在逼近翻轉**。
   若某段翻轉（read > commit），那是 R4 款 3 失守 ⇒ 實作端會停手回報。
2. **後排雙方同時下滑**：read 84.4→67.4、commit 68.1→57.4（方向仍對）。

---

## 四、本段修掉三處**探針硬編碼**（與 A-7／A-8 同源，主動申報）

| 探針 | 問題 | 已修 |
|---|---|---|
| `phase5-reach-shrink-probe` | P1(a) 的歸一化分母**自己用 t=0 的式子重算** ⇒ 基準 B 之後包絡是**逐球員逐段**的，分母取錯——**正是閘門第 3 條要抓的東西** | 改向 `reachRadiusFor(action, TUNING, h)` 要 |
| `phase5-block-width-probe` | 密合率**硬編碼列 `[1.1, 0.5]`** ⇒ 漏掉「當段實際半寬」（段 1 是 **0.950**，密合 100%） | 改吃 `TUNING.BLOCK_REACH_X` ＋終點 0.5 |
| `phase5-passtier-probe` | 表頭印「舉球可及現值 1.300」＝印了 `REACH_RADIUS` 而非收斂後值 | **未修，如實登記**（只影響顯示，計算未受影響） |

⇒ 教訓與 A-7／A-8 同源：**量測工具也會把「沿用的量」當成不變量**。
凡會被旋鈕移動的量，探針一律向單一真相來源取，不得自己重算。
**建議把這條寫成附錄 A-9**（若裁定端認可）。

---

## 五、誠實聲明

1. §一 全部是實測，指令見 §六。
2. §二① 的「基準 A 時 group-1 是 95%」是**未配對**的絕對值，僅供理解方向，
   **不作證據**（§4.3：判準必須用配對差值）。
3. §二② 的「d 的 95 分位 ≈ 0.48」是由 p90 0.441／max 0.556 內插推得，非直接量測分位數。
4. 段 1 的 P6（身高三臂）**未跑**——三臂是 §1.4 的哨兵，依 B-1 要在**收斂完成後**以配對、
   n≥100 重測；段間跑 n=24 只會得到雜訊量級的數字（附錄 §2.2 已判過同一件事）。
   **如實登記為未跑，非遺漏。** 若裁定端要求逐段跑，請明示。
5. 本檔**未送 fresh-context 冷讀**。v4 §五 已恢復冷讀機制；本檔為四項並列的複合送裁
   ⇒ **實作端建議送冷讀**（v4 §五 清單第一項＝語意與原點一致性檢查，正好適用 §零 的內插空間交代）。

---

## 六、驗證指令

```bash
git checkout main   # 56887a3
npm test                                          # 781／780 綠／1 紅（＝送裁項 ④）
node tools/sim-hash-probe.mjs                     # ✅ 05384976bf4dd961
node tools/phase5-reach-shrink-probe.mjs 10       # P1(a)(b)(c)：貼邊率、逐球員上限、rally 長度
node tools/phase5-passtier-probe.mjs 10           # P2 ＋ §二② 的 d 分佈
node tools/phase5-block-width-probe.mjs 8         # P3（會印「當段實際半寬 0.950」與終點 0.5）
node tools/phase5-block-geometry-probe.mjs 40     # P4 E 表
VD_PAIRED=tools/balance-paired-baseline-A.json node tools/balance-sim.mjs 40   # P5 配對Δ

# 機制自驗：把 constants.js 的 CONVERGE_T 改 0，應逐值回到基準 A
#   node tools/sim-hash-probe.mjs → 06343a6e17904c4e、npm test → 781 全綠
```
