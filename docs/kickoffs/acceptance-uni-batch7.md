# 大學卷 批 7「兩招新技術」— 驗收條件（2026-08-15 動手前訂定，此後凍結）

> 依 `02 §2.1`：一經訂定即凍結，修不過去要改的是實作。
> 上位卷宗＝`docs/kickoffs/university-chapter-kickoff.md`（§三之三、§四 實作順序第 7 項）。
> 前置＝批 1–6（章節狀態／升學評定／治具／屆數章節化／九校＋升學畫面／大學長循環賽季）。

## 動手前拍板（2026-08-15 Sawmah）

1. **壓手攔網的代價＝提早賭**（不動 sim）。壓手鈕只在**早窗**有效，過了就只剩一般攔網。
   代價由 sim 既有的跳躍相位曲線給：`blockTopEdge`（`src/sim/player.js:128-133`）是正弦弧，
   airT=12 為頂點、>24 回站立摸高 ⇒ **早起跳＝球到時手已下墜，構不到就碰不到**。
   ★不是我們外加的懲罰，是既有機制★
2. **追發＝發球面板加一層**：現有四落點區照舊，多一個「追發」入口 → 展開對方**後排三人**。
3. **傳授＝大學章專屬事件，技術軸打平**——所有學校**同場次**學到（不分強弱）。
4. **強弱校差異只靠球權＋戰績**（兩軸都已在批 6 做完）。
   ⇒ **升學畫面不得再寫「技術解鎖快」**，那是階段一兌現不了的支票。
5. **一次做完，不拆批。**

### ★ 動手前量測（已完成，數字驅動設計）★

用 `tools/retract-value-probe.mjs` 跑真實 sim 路徑（1750 局、36,150 次攔網接觸）：

- 擦側（被打手）之後攔網方**直接失分 39.2%**（n=5274），其中真正出界僅 **3.8%**
  ⇒ `src/sim/game.js:1296` 註解那句「~6%」偏高，且只講了出界，
  漏掉大宗＝**球落界內沒人救到（35.4%）**。★這行註解本批順手更正為實測值★
- 壓手對直立：擦頂子樣本 39.3% → 78.6%（n=28 偏薄，方向明確、倍率不當精確值）

> **retract（縮手）第三檔的去留＝見下方「B7-9」**（2026-08-24 已填：不做）。
> ★上面「量 2」的舊數字量錯位置（量成「整面牆消失」而非「玩家一個人縮手」），
> 已於 2026-08-24 重跑修正版，結果與 B7-9 的依據全在
> `docs/kickoffs/uni-batch7-retract-measurement.md`★

## 這一批要交付什麼

1. `TECH_DEFS` 加兩招（`growth.js:33-52`）：壓手攔網、追發。
2. **壓手玩家端**：攔網決策時刻的早窗多一個「壓手」輸入 → intent 帶 `hand: 'press'`。
3. **追發玩家端**：發球面板「追發」入口 → 對方後排三人 → 換算成 aim 世界座標。
4. **大學專屬傳授事件**：第 3 場後→壓手、第 6 場後→追發，各校一致。
5. 升學畫面文案修正（拿掉技術解鎖承諾）。
6. 配套三處（不補會靜默壞掉，來源＝動手前盤點）：
   `practiceMatch.js:399` `TECH_DRILL_ORDER`（漏加＝新招永遠沒有集訓科目，**不報錯**）、
   `events.js:339-344` `ONCE_EVENT_IDS`（漏加＝舊存檔重播教學事件）、
   `careerScreen.js:1487-1498` 的直寫 unlock 路徑（與 `growth.js:130` 平行維護）。

**本批不做**：身高曲線 U4（批 8）、大二以後、大學招募、
生涯數據頁把第 3 屆標成「（進行中）」的顯示問題（批 6 已記）。

## 驗收條件（機械可判；括號內＝什麼樣的實作會讓它變紅）

- **B7-1 兩招進技術樹且預設未解鎖**：`TECH_DEFS` 恰 **9** 條，新增兩條 key 唯一、
  有 `name`／`desc`；新建生涯的 `player.techniques` 這兩鍵＝**0**。
  ★反向對照：既有高中存檔載入後這兩鍵＝0，**不是 undefined、也不是 1**★
  ▸ 紅法：直接給滿，或忘了進 `normalizeCareerPlayer`（`careerState.js:472-497`）
  導致舊存檔讀出 `undefined` 而 UI 當成已解鎖。

- **B7-2 壓手真的送到 sim（不是死按鈕）**：★**必須實際 tap 按鈕**，不得只驗文字在★
  按下壓手 → 該次攔網窗的 `actor.blockHand === 'press'`。
  ★反向對照：不按壓手（走一般攔網）→ `blockHand === 'vertical'`★
  ▸ 紅法：UI 畫出按鈕但 intent 沒帶 `hand`（`game.js:596` 的 `?? 'vertical'` 會靜默吃掉，
  畫面上完全看不出來）。這正是批 5 教訓 6 與批 6 教訓 10 的同型陷阱。

- **B7-3 壓手有代價（早窗限定）**：壓手輸入**只在早窗有效**；早窗關閉後按壓手
  必須退回一般攔網（`blockHand === 'vertical'`），不得仍送出 `press`。
  ★反向對照：早窗內按＝`press`，早窗外按＝`vertical`，兩者逐值不同★
  ▸ 紅法：壓手鈕全程可按 ⇒ 純增益、沒有抉擇（＝這一招在騙玩家）。

- **B7-4 未學會不得使用**：`techniques.pressBlock < 1` 時壓手輸入**不存在**
  （鈕不出現且鍵位無效）；`chaseServe < 1` 時發球面板無「追發」入口。
  ★反向對照：解鎖前後同一情境，選項數不同★
  ▸ 紅法：只把按鈕變灰但按下去照樣生效（gate 寫在顯示層而非行為層）。

- **B7-5 追發發得到對的地方**：選對方後排某人 → 發球 aim 世界座標＝該員站位換算；
  **選不同的人 → 座標不同**。
  ★反向對照：三個目標的 aim 三個不同值★
  ▸ 紅法：三個選項都發到同一點（追發形同虛設，畫面上看不出來）。

- **B7-6 追發名單隨輪轉變動且只列後排**：列出的恰為對方**當前輪轉的後排三人**；
  對方輪轉推進後名單跟著變。
  ★反向對照：輪轉前後兩份名單不同★
  ▸ 紅法：寫死三個名字或直接列六人（後排限制來自 `ai.js:3006-3013` 的 `isBackRow` 語意）。

- **B7-7 傳授走大學章且各校一致**：大學第 3 場後解鎖壓手、第 6 場後解鎖追發；
  **選強豪與選弱校，解鎖場次相同**。
  ★反向對照：跑兩份不同 tier 的存檔，解鎖場次逐值相等★
  ▸ 紅法：沿用 `when.lastMatchId` 綁高中賽事 id ⇒ 大學永遠觸發不到，玩家一年學不到任何東西。

- **B7-8 升學畫面不再承諾技術解鎖快**：升學畫面文案**不含**「技術」相關的快慢承諾。
  ▸ 紅法：留著空頭支票（卷宗 §三之二自己寫的「不是來自他被騙」）。

- **B7-9 縮手（retract）第三檔＝不做**（2026-08-24 依重量結果填入，Sawmah 拍板）：
  本批 diff 中**不得出現任何玩家端 retract 輸入路徑**——玩家端送出的 `intent.hand`
  取值域恰為 `{vertical,press}` 兩值，`retract` **僅 AI 可產生**。
  ★反向對照：grep 玩家輸入層（`matchControls.js`／攔網決策路徑）無 `retract` 字面；
  且 AI 側仍產得出 `retract`（既有行為不變）★
  ▸ 紅法：加了縮手鈕。依 `docs/kickoffs/uni-batch7-retract-measurement.md`，
  玩家一人縮手 → 隊友補上率僅 6.8%、這一分攔網方勝率 50.9%→5.8%（−45.1pp 顯著，n=637），
  ⇒ 純代價無增益＝永遠不該按的假抉擇，正是 B7-3 要避免的反面。

- **B7-10 零外溢**：`src/sim/**` 在本批 diff 中**零改動**（註解更正除外，見下方例外）；
  `node tools/sim-hash-probe.mjs` 逐值＝`2f60f04312b666db`；
  `src/career/schedule.js`、`src/career/opponents.js`、`src/career/uniSchedule.js` 零改動；
  既有測試檔除下方例外外**一行未改**。
  ▸ 紅法：為了讓新招生效去動 sim 的攔網或發球結算。

  > **例外一項（待使用者追認，`02 §2.1` 程序）**
  > `tests/growth.test.mjs:80` 的 `assert.equal(TECH_DEFS.length, 7)` 要改成 `9`。
  > **原標準錯在哪**：它不是「錯」，而是把「現在恰好有 7 招」寫成了不變量；
  > 本批的交付內容就是加兩招，這條與 B7-1 不可能同時成立。
  > **難度不變**：仍是逐值相等斷言，不是放寬；`7→9` 不會讓任何一份壞掉的實作通過
  > （少加一招、加錯 key、加了但沒進表，都仍然紅）。
  > **使用者追認**：**2026-08-24 Sawmah 明確同意**（針對這一條，理由如上：仍是逐值相等斷言，非放寬）。

- **B7-11 高中零回歸**：既有高中存檔的行為與本批實作前逐值相同
  （沿 B1-2／B4-4／B5-9／B6-8 的既有測試，不得改那些測試）。
  ▸ 紅法：把兩招也掛進高中傳授鏈，導致高中三屆的技術進度改變。

## 人工驗收（部署後試玩）

- **P7-1**：`?devseed=champion&devslot=3` → 選北陵（強豪）→ 打到第 3 場後
  看得到「學會壓手攔網」→ 下一場攔網時早窗按得到壓手，且**擦頂真的把球壓下去**。
- **P7-2**：同一份治具打到第 6 場後 → 發球面板出現「追發」→ 展開看得到對方後排三人的名字
  → 發過去，球真的飛向那個人。
- **P7-3**：改選梅溪（弱校）→ **解鎖場次與北陵相同**（技術軸打平的人工確認）。

## 鑑別力要求

本批最容易「看起來有、其實沒有」的是 **B7-2／B7-3／B7-5** ——
三條都是「UI 出現了但值沒送到 sim」，畫面上長得一模一樣：

- B7-2／B7-3 的紅法會被 `game.js:596` 的 `?? 'vertical'` 靜默吸收
- B7-5 的紅法（三個目標同一座標）在畫面上就是「球都往那邊飛」，看起來很合理

⇒ 這三條的驗收**一律要 tap 下去看實際的值**，不得只驗文字或 DOM 存在。
（批 5 教訓 6「只驗文字在抓不到按了沒反應」、批 6 教訓 10「沒有測試點過的按鈕就是盲區」）
