# 大作感三卷 kickoff（2026-08-30）

承大作感二卷（08-30）。08-30 晚提案完畢、Sawmah 拍板「等等開卷」，批次順序照建議：

- **批1 觀眾反應動畫**：得分死球觀眾起立/彈跳波動，關鍵分加碼「燈海」（觀眾席光點）。
  觀眾現況＝單一 InstancedMesh 一次性矩陣（arena.js buildCrowd），需開對外把手。
- **批2 賽後 MVP 演出**：燈暗聚光＋金字卡＋三數據。資料＝box score 現算（boxScore.js）。
- **批3 發行商開場 logo**：SAWMAH GAMES presents，可跳過，只在開機演一次。

後補（試玩完再議）：LED 廣告圍欄（虛構品牌跑馬燈）、關鍵分應援節奏拍手（合成，勿用
真實素材——哨音教訓）、照片模式。

## 驗收凍結（動手前訂，改動照 02 §2.1；開卷基準 main 56b70fc / gh-pages 01a8665）

通用：`src/sim/**` 零改動且 sim-hash 前後逐場同；全套 `npm test` 綠（基準 2352）且不改
任何既有測試；部署 gh-pages 附送達證明；視覺效果由 Sawmah 試玩裁定（誠實條款），
數值標【試玩必調】。

### 批1 觀眾反應動畫
- K1-1 得分死球觸發觀眾反應動畫（起立/彈跳波動），反應窗結束觀眾回到原始靜止姿態
  （矩陣還原）；反應窗外零 instance 矩陣更新（效能鐵則）
- K1-2 關鍵分死球加碼燈海（觀眾席光點層）：與 sfx 同源讀 `s.keyPointRally` 鎖存值，
  不另寫第二份關鍵分判定；普通得分不出燈海（雙向）
- K1-3 反應包絡寫成純函式（吃 elapsed＋keyPoint → 強度/燈海開關），雙向單元測試：
  窗內強度>0、窗外=0、keyPoint 窗長與強度 ≥ 普通
- K1-4 動畫全掛 render/UI 層；崩潰 try/catch 退回靜態觀眾，永不致死
- K1-5 既有觀眾建置行為不變：建館後未發生得分前，畫面與現行一致（矩陣一次性設定不變）

### 批2 賽後 MVP 演出
- K2-1 生涯正式賽結束觸發 MVP 演出（勝敗皆演；MVP＝勝方 box score 最佳）；練習賽/
  教學局/連線對戰不演。演出含：燈暗聚光（複用 scene.js 調光範式）＋金字卡（theme.js
  tokens，不走 championBanner 寫死 hex 的舊路）＋至少三項數據
- K2-2 MVP 選定寫成純函式（吃兩隊 box rows＋勝方 → {pid,name,數據}），雙向測試：
  取勝方最佳／rows 空回 null（null＝不演直接走現行流程）
- K2-3 點擊/觸碰可跳過；跳過或播畢都落回既有 setOverOverlay 流程，生涯層收到的結果
  完全一致；與冠軍慶祝相容（慶祝先、MVP 次之，各自可跳）
- K2-4 燈暗演出結束（含跳過）燈光完全還原比賽常態值
- K2-5 結算先於演出、演出崩潰 try/catch 落回現行流程（沿二卷 J1-5 永不致死慣例）

### 批3 發行商開場 logo
- K3-1 開機顯示「SAWMAH GAMES presents」logo 頁（純 DOM/CSS 動畫），自動播畢或
  點擊跳過即消失；一次開機只演一次
- K3-2 logo 頁不阻塞開機（底下載入流程照跑）；建立失敗 try/catch 不影響開機
- K3-3 消失後主選單/attract 照常運作（logo 蓋最上層，z-index 高於 #loading）
- K3-4 播畢/跳過判定寫成純函式＋單元測試（elapsed 未滿且未點擊→留、滿或點擊→走）
