# Phase 4 W4 討論簡報（實作端 → Claude.ai；2026-07-27）

> 目的：生成 `phase4-w4-implementation-prompt.md`（W4＝賽制＋場館＋數據＋結算——
> 憲法 Q5/Q8/Q9/Q10；「大日子要像大日子」）。
> 本檔由實作端（Claude Code/Fable）整理——只陳述現況事實與待拍板題，不代做設計決策。
> 配套附件：`phase4-w3-status.md`（W3 結案快照，含當日試玩迭代與九題待拍板全文）、
> `phase4-decisions-RESOLVED.md`（憲法）、`phase4-week-plan.md`（W4 段）。

## 1. 現況一句話

W3 五位置工程全關卷且**當日試玩迭代完結**：`main@307fd50`、**479 測綠**（基線 439，
只增不減）、已 deploy。與前兩週不同——**本週 Sawmah 已完整試玩並當日拍板七輪迭代**，
位置手感體感過關、四位置已手批全開、第 3 屆 L 新生已選案落檔（B 案紀慕白）。
試玩債只剩演出規格類（累積於快照 §11 優先級 2/3）。

## 2. W4 工單需要知道的工程事實

1. **位置玩法最終形態（試玩迭代後定稿）**：S＝分配面板（一傳三分支池＋猶豫標快攻）；
   MB＝站位全手動＋面板一顆「🧱起跳攔網」（時機全程玩家控）＋時機真值字卡；
   OPP＝OH 鏡像；L＝收縮指揮（三選項/建議◎/1 秒自動/改判、偏移 1.3m）＋讀對字卡
   ＋Perfect 窗＋習慣標記＋魚躍演出。決策可視化（確認浮字＋結果字卡）全上線。
2. **導師介面契約已立**（`mentor.js`）：四規則決定論、**box score 輸入契約＝
   `{ sets:{pid:n}, names:{pid:名}, keyPoint:{sets,kills}, consecErrors, result:{won} }`**
   ——W4 記帳端供此形狀即可接線（掛點＝賽後鏈、玩家=S 時）。
3. **L 三欄帳已逐場入檔**：`stats.liberoBox={digs,assistDigs,rallySaves}`
   （`boxScoreL.js` 事件流攔截）——W4 數值頁直接消費；玩家 S 分配數/改判成功率
   等欄位待 W4 記帳端定形。
4. **A7 替補 OH 阿遠已入創隊名冊**（grade 2、體力 72）：第 1 屆板凳/換人/板凳快回
   閉環可用；**第 2 屆末畢業者＝阿岩＋阿遠兩人**（畢業儀式演出量 ×2，W4 若做結算
   相關演出要知道）。capacity 維持 12（招募空位 4）。
5. **快速比賽＝位置遊樂場**（主選單選位/`?role=`）——W4 的多局制/場館若要快速比賽
   也吃得到，設定入口在 `matchConfig.buildQuickSetup`。
6. **平衡基準（rowFactor 0.5 權威版，n=150）**：175 主錨 23%/7%（W2 錨點復位）；
   位置臂＝S 33%/5%、MB 25%/3%（攔網歸因 1.00/場首記）、OPP 17%/2%、L 32%/4%；
   L 改判空間＝suggest 23%→omni 30%（+7pp，附錄 A5 初擬帶下緣）；150-L 23%
   vs 150-OH 8%＝矮個之路兜底實證；**「不指揮」32% 高於「照建議」23%**（建議品質
   /偏移代價的設計訊號）。治具支援 `VD_ROLE`/`VD_L_MODE`。
7. **體力現況**（W4 多局制的直接輸入）：單局長 rally 主角體力可見底（Sawmah 實測）；
   恢復手段＝死球 0.005/板凳 0.025/暫停 0.03；體力不可加點（僅屆間訓練營 +2 上限 80）；
   對手 costMul 0.6＋heavyExempt。Q8 拍板的跨局延續＋局間恢復會放大壓力。
8. **演出框架消費者現況**：heightRitual＋graduationRitual 兩個；來投開箱/生涯結算
   （Q5 對標來投以上）待接。ritualStage 把手＝setSpot/setHeight/setPose＋tween。
9. **schema 現況**：`career.positionFlags` 已用（四位置 open）；`story` 頂層鍵仍空
   （「不要猜」）；存檔仍單槽（多槽=待拍板題 3）。

## 3. 待拍板題（快照 §0b 全文；此處摘要＋實作端傾向）

### 題 0｜W4 工單範圍確認
憲法排程＝分級賽制（Q8 全配套：三局兩勝/五局三勝/決勝局 15、體力跨局＋局間恢復、
氣勢跨局歸零、局間 huddle、**局間存檔必配**）＋三館制（Q10）＋box score（Q9）＋
生涯結算（Q5 對標來投以上）。範圍溢出時犧牲順序沿週次總覽（結算演出精緻度＞場館
細節＞其餘不動）。下列追加題要不要塞進 W4 或排 4.5，請一併裁定。

### 題 1｜轉位時機（快照 §0b-1）
屆間制（現制，三屆僅 2 次機會）vs 賽季中「主動請調」。實作端傾向折衷：屆間談話
保留＋賽季中主動找教練的對話事件（open＋每屆一次＋打滿 N 場）——注意 Q7「劇情事件
驅動非選單按鈕」語意邊界。

### 題 2｜本地多存檔槽（§0b-2）
3 槽＋選檔畫面提案；與轉位重玩性互補。憲法 C 方向緩議的是「雲端」，本地多槽不受限。

### 題 3｜玩家 S 二次球偷襲（§0b-3）
S 分配面板前排＋完美一傳時多一顆偷襲選項（沿 AI dump 同 sim 路徑）——S 的「自己
出手」時刻。拍板：做不做/出現條件/風險回報。

### 題 4｜L 玩法 2.0——攔防對位（§0b-5，**實作端強烈推薦**）
Sawmah 提案：L 指令升級成攔防配套（攔網封直線＋後排收斜線等組合，前排攔網站線
跟指令走）＝真實防守體系、「防守指揮」名實相符。工程可行（blockScheme 沿 digBias
架構）；動附錄 A 定案範圍故須重拍板。若做，治具 L 臂重測。

### 題 5｜OPP 專屬「一招」（§0b-7）
現=OH 鏡像。候選：後排 D 球時機自選／右翼單人攔網一對一博弈。OH 不再加面板
（動作型位置爽感靠攻擊本體＋box score 數據文化）。

### 題 6｜世界觀大戲軸（§0b-8）
「大作應該要有」的 rivalry 人物大戲——C 案兄弟隔網對決概念的移植地。候選掛點：
對手 ace 宿敵線深化／Q5 生涯結算關鍵一戰／Q10 關鍵戰館戲劇配套。裁範圍與掛點。

### 題 7｜體力管理手段（§0b-9）
多局制壓力放大下，局內手段要不要擴充（開放體力加點/暫停回復量/其他）——
與 Q8 五局制體力治具輪一併裁。

### 題 8｜平衡遺留細裁（§0b-9＋快照 §4）
S 臂 33% 偏高/OPP 臂 17% 偏低要不要調、D 球全局寫實化（0.65 會拖出錨點、要整組
重校準）要不要付成本、L 改判空間 +7pp 要不要拉大（候選：收縮速度加成/Perfect 增益
/提早線索/建議吃標記比重）。

### 題 9｜演出規格輪排程（快照 §11 優先級 2）
攔網雙路線重量感（已定調）＋甲2 遲疑/果斷骨架動畫＋魚躍塵土/rally 終結鏡頭＋
儀式「大作」規格——排 W4 內、W4 尾、或 4.5？

## 4. 慣例提醒（工單模板照舊）

自包含文件、引用憲法不重述、明確 out of scope、驗收清單 Sawmah 可獨立驗證、
**479 為新基線只增不減**、命名相關先跑 naming.test、地基級（多局制 sim 狀態機/
跨局體力/局間存檔/三館程序化）用 Fable 深推理、決定論鐵律、sim 純核心零 three.js/DOM、
Intent 唯一輸入、手機 60 FPS、需試玩＝commit＋deploy:pages、結案產出
`phase4-w4-status.md` 權威快照（含試玩批次清單滾動版）。
