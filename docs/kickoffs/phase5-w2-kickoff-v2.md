# Phase 5 W2 kickoff v2 — 表現層動作真實化（收斂後版）

> 2026-07-30 產出。取代 `phase5-w2-discussion-brief.md` 的討論基礎（該檔 5 組 14 題
> 寫於 §十 之前，前提大量過時——逐題鮮度判定見 §一）。
> 時點：§十「讓 sim 誠實」三件套全卷結案（convergence §七）；Sawmah 拍板 W2 先於十-4。
> 鐵則沿用：表現層不動 sim（sim-hash `fe42e8962c5e7523` 為基準）；動 sim 的掃尾項
> 逐項標明並單獨驗證。

## 零、地基變動（判定所依據）

| 變動 | 對 W2 的意義 |
|---|---|
| 觸球判定＝手點球心球體（`reach.js`），收斂 t=1（接 0.38H／舉 0.45H／扣 0.55H） | 「球離手很遠也算碰到」的時代結束＝**視覺補償層的存在前提消失** |
| 補償終態：貼邊率 接/舉/扣 全 0.0%（convergence §5.12⑧） | 動畫手掌 vs 球心的落差主體已被 sim 端吃掉 |
| S1 一傳分檔活化（87.6/7.7/4.8）＋跳舉執行率 8.5%→**29.3%** | 跳舉「隱形動作」從邊角債變成高頻債 |
| 攔網帶模型（半寬 0.5、縫＝站距−1.0、solid/graze/clean 三態） | 攔網演出與新幾何未對齊（張臂寬、縫、三態無視覺分別） |

## 一、舊 14 題鮮度判定（結論；證據在盤點紀錄）

- **已解結案（3）**：B-1（MB 一速跳躍）｜C-3（接球高手/低手分辨——幾何解掉：接球
  球體上緣 1.52m@H1.75 < OVERHAND_Y 1.6 ⇒ 高手接球構造上打不到；殘留＝舊碼清理）
  ｜D-3（一傳恆 perfect——S1 解）
- **退場（1）**：D-2（奪冠率偏低）→ 已由補償 ±15 收單吸收，退為實玩體感複驗
- **改寫（4）**：A-1/A-2/A-3 三個口味值 → 併成新題「reachAssist 退場案」（§三 裁定題 1）
  ｜C-2 二速 → 新題「解封重測與歸屬」（§三 裁定題 2）
- **仍成立（5）**：B-2 假動作全員演出｜B-3 假動作收勢｜C-1＋C-5 併題（鞭打切分）
  ｜C-4（一速提前量三數字全需重測）｜D-1（dig 目標無場地夾限——sim 小修掃尾）

## 二、W2 工作清單（三層，供挑 scope）

### W2-核心（W1 結案明列的三債務，必做候選）

1. **完整鞭打切分重設計**（C-1＋C-5 併題）：`spike` 序列 0.30 spikeWind→0.36
   spikeUnlock→0.42 spikeHit 三個解鎖幀被「空中接續 cap=hit」整段跳過——玩家從沒
   看過完整鞭打。W1 已證 `windup`/`spike` 兩弧相位在不破壞跳躍連續性下無解 ⇒
   需要新的切分方案（討論題，見 §三 裁定題 4）。
2. **假動作三步助跑全員接線**（B-2＋B-3）：sim 全員各跑各線，演出只發
   `attackerId`/`claimId` 兩人 ⇒ 誘餌在畫面上是普通跑步＝欺敵武器無戲。含收勢
   （落地轉防守）演出。
3. **跳舉表現層**：`jumpSet` 在 render 零消費者、`overhead` 序列 jump:0——
   每三次舉球一次隱形跳躍（29.3%）。治具現成（`phase5-jumpset-probe`）。

### W2-補課（範圍預告缺項，挑選制）

4. 攔網演出對齊新帶模型：張臂寬 vs 半寬 0.5、縫的可視化、solid/graze/clean 三態
   視覺分別（現在三態同畫面）
5. 慣用手視覺鏡像：POSES 一律右臂擊球、壓腕只給右手；步序已分（資料面就緒）
6. 二次攻擊專屬演出（假傳真扣的欺敵動作——注意 §十二「假扣真傳」獨立卷，別越界）
7. 接球滾翻（範圍預告項，全域零實作）
8. 發球兩型細節（跳飄/跳發骨幹已有，只剩收尾）

### W2-掃尾（小、雜、部分動 sim 要單獨驗）

9. **reachAssist 退場案**（§三 裁定題 1 的執行）
10. C-3 舊碼清：`contactSeqFor` overhead 接球分支＋`matchLoop` 的 1.35m/20.8% 舊註解
11. D-1：`digTargetFor` 回傳加場地夾限（動 sim，比照 `clampCourtX`；單獨驗證）
12. C-4 三數字重測（`SET_CONTACT_BIAS 11`/提前量名目 vs 實際）——先量再決定要不要動

## 三、裁定題（Sawmah）

1. **reachAssist 退場案**：收斂後補償層前提消失（憲法 §一.2 預告「從化妝變成不需要」；
   貼邊率全 0 實證），但該檔一行未動、註解仍寫舊幾何。選項：甲＝全退場（先跑
   `contact-frame-probe` 重測落差，確認視覺無跳變後拆）／乙＝保留水平只關垂直／
   丙＝三值歸零留殼。前置＝重測一次落差，數字出來才裁。
2. **二速歸屬**：阻塞論證的接觸模型已被 §十-1 換掉（憲法預告「自動解封」），但
   `TEMPO_TWO_RATE=0` 與兩道舊閘未動、收斂全卷零重測。裁「解封重測」歸 W2（表現＋
   解封）還是十-4（彈道卷一起）。
3. **scope 挑選**：核心 1–3 全做？補課 4–8 挑哪些？（建議：核心全做＋補課 4、5；
   6 等 §十二、7/8 視體感）
4. **鞭打切分的方案方向**（開放討論題）：候選＝①三段式（助跑弧／滯空 hold 相位／
   擊球弧）②動態 cap（接手點依剩餘滯空時間回推，短滯空才跳幀）③擊球幀回移
   （hit 提前、後段全給 follow）。W1 的失敗解法（兩弧直接對相位）已證無解，勿重走。

## 三之二、裁定回填（2026-07-30，Sawmah，AskUserQuestion 四題）

1. **reachAssist＝保留＋對齊校準**：退場案被 `reach-probe` 實測否決（修後手→球心仍
   p50 0.61–0.75m、垂直主導；補償層每球實縮 0.15m＝還在幹活）。憲法 §一.2「不需要」
   的預告＝拿貼邊率（sim 殼層座標）推視覺座標的 A-8 跨座標誤推，**以本節為準勘誤**。
   工作＝常數改吃 `reachRadiusFor` 單一真相＋舊註解重寫；LIFT_MAX 飽和（set 0.17/0.18）
   鬆不鬆由試玩定。
2. **二速解封重測＝歸 W2**（節奏檔非彈道；重測舊閘→解封試派→失敗登記回報）。
3. **scope＝核心三債＋掃尾四項＋補課④（攔網演出對齊帶模型）⑤（慣用手鏡像）**；
   ⑥二次攻擊等 §十二、⑦滾翻視體感後補。
4. **鞭打切分＝①三段式方向**（助跑弧／滯空 hold 相位／擊球弧；hold 相位模式已有
   receiveReady/sustain 先例）；原型做出來以試玩檢查點定案，W1 的兩弧對相位死路勿重走。

## 四、驗收框架（實作時逐項細化）

- 表現層項：sim-hash 不變＋`npm test` 綠＋觸球幀對齊治具（`contact-frame-probe`）
  ＋試玩檢查點（工作流程約定：直接更新上傳）
- 動 sim 的掃尾項（11 等）：02 §6.1 鑑別力流程（修前紅在行為斷言）
- 跳舉演出：`phase5-jumpset-probe` 的執行率不因演出接線而變（演出零 sim 副作用）
- R4 款 3 預警照舊：W2 不動 AI 時序 ⇒ 原則上不觸；若任何項碰到，先重跑 P4 快攻列
