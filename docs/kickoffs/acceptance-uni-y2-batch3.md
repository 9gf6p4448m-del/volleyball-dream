# 驗收凍結——大二卷批 3：名次帶著走＋信任微調（2026-08-25）

拍板依據＝題 2 甲（名次→下一年起始信任微調＋數據頁歷年名次紀錄，不動對手強度）。
加成表（★數值屬提案，試玩後可調——比照配色卷色票慣例★）：
聯賽第 1＝+6／第 2-3＝+4／第 4-6＝+2／第 7-9＝+0；上限 clamp 100。
放 `uniTeam.js`（與 UNI_START_TRUST 同檔＝球權座標系單一處）。

## B1-4 改寫聲明（02 §2.1 程序）

批 1 的 B1-4 凍結「推進前後 `fromSetter` 逐值不變」與**使用者拍板題 2**（名次微調）
直接矛盾——凍結時寫過嚴：批 1 尚未實作微調，誤把題 3 的「不重置」寫成了「不變」。
拍板在先、位階更高，B1-4 改寫為：「`floorShare` 不變；`fromSetter`＝原值＋名次加成
——不歸零、不重設回 UNI_START_TRUST」。壞版對照：把 fromSetter 重置回起始值的
實作必紅。此改寫將在收尾回報向使用者點名。

## 驗收條件（凍結，改動依 02 §2.1）

- **B3-1 封存欄位**：大學屆推進後 `career.seasons` 末筆含 `uniRank`（1-9）與
  `school`；高中封存逐值零改動（不帶新欄位）。
- **B3-2 信任微調**：推進後 `fromSetter = min(100, 原值 + bonus(uniRank))`；
  `floorShare` 不動。★改前紅★：批 2 完成點（a6aaebc）上 bonus 不存在 ⇒
  「推進後 fromSetter 上升」的斷言必紅於行為斷言（worktree 實跑紀錄）。
- **B3-3 數據頁**：大學屆卡片顯示「聯賽第 N 名」（第 1 名＝🏆 聯賽冠軍、計入
  生涯合計 🏆×N 顯示）；高中屆卡片渲染逐字不變。
- **B3-4 決定論**：同存檔重演推進，封存的 uniRank 與加成後 fromSetter 逐值同。
- **B3-5 全套綠（基準 1702 不得少，B1-4 改寫除外）＋sim-hash 同基準。**
