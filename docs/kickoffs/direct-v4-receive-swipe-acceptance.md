# direct-v4 接球滑動平台角度：驗收條件（2026-09-24 動手前凍結）

使用者裁定（全部照建議）：①四向離散，同扣球手勢 ②上＝高球到位、下＝低平安全球、左右＝偏左偏右、不滑＝平台正對身體 ③觸球窗口開始即鎖定 ④入門＋標準顯示平台朝向線與接球時機提示，進階不顯示；提示不改判定 ⑤訓練新增「接球方向練習」，地上標目標區，只顯示結果、不保底。

本檔凍結後，任何會讓通過機率上升的修改（降門檻、縮案例、改量測方式）須先寫明原標準錯在哪，並取得使用者對該條的明確同意。

## 模擬核心（`src/sim/`，決定論）

- **A1 指令契約**：指令欄位 `passType` 的值只能是 `HIGH`／`LOW`／`LEFT`／`RIGHT`／`NEUTRAL`。只在「同 tick 開始墊球」或「墊球準備期（actionTick < windup）」時採用；其他時候（出手窗口後、非墊球動作、非法值）一律忽略。按下墊球沒帶 `passType` 就是 `NEUTRAL`。單元測試逐條驗。
- **A2 連續與鎖定**：平台角度在準備期連續過渡，每個 substep 的角度變化 ≤ 設定速率；從出手窗口第一個 tick 到動作結束，平台角度逐值不變（測試逐 substep 取樣）。
- **A3 物理效果（真實鏈路）**：用正式 `feed(receive)`，玩家站在固定位置，只改 `passType`，同一按鍵時機，每種都要產生主動（active）的前臂或手部觸球。量測觸球後的球速或弧頂：
  - LEFT 的出球 vx ≤ NEUTRAL 的 vx − 0.5 m/s（往玩家左側，也就是 −x；玩家面向 −z）；RIGHT 的 vx ≥ NEUTRAL + 0.5 m/s。
  - HIGH 觸球後弧頂 ≥ NEUTRAL 弧頂 + 0.3 m；LOW 觸球後弧頂 ≤ NEUTRAL 弧頂 − 0.3 m。
  - 以上須在至少 3 個站位（x = −0.3、0、0.3）都成立。
- **A4 不放大、不傳送**：各 `passType` 的姿勢，膠囊數量、半徑、active 旗標都和 NEUTRAL 相同；球速只在碰撞時改變。同一球若沒有碰到身體，不論選哪種 `passType`，整段軌跡都和 NEUTRAL 逐位元相同。
- **A5 決定論**：含 `passType` 的錄影，整卷重播與逐 tick 還原都逐位元相同；`direct-v3` 錄影明確拒絕。
- **A6 不加衝量**：出手窗口內，平台角度的變化對表面速度的貢獻為 0（由 A2 的鎖定保證，另加一條測試：晚到準備期最後一個 tick 才選，觸球後球速與「準備期結束時角度」一致，不含轉角速度）。

## 輸入

- **A7 手勢**：觸控出手鈕在墊球時滑動會送出對應的 `passType`（分類規則與扣球四向同一套方向判定：上、下、左、右、不滑）；原本的左右滑改朝向行為保留。鍵盤墊球送出 `NEUTRAL`。有自動化測試（沿用既有輸入測試的方式）。

## 畫面與訓練

- **A8 平台朝向線**：入門與標準模式在墊球準備期與出手期顯示；進階不顯示。朝向線由共享姿勢的前臂計算，不另算物理。
- **A9 時機提示**：入門與標準模式在墊球可觸及的來球接近時，出手鈕外圈收縮；進階不顯示。只有畫面，不改任何模擬輸入。
- **A10 接球方向練習**：餵球選單新增「接球方向練習」，使用正式墊球餵球（模擬核心不新增分支），地上標示目標區；球落地後顯示命中，或落點相對目標的偏移（公尺）。不改變球的任何速度與位置。
- 以上 A8–A10 由瀏覽器治具在 1280×720、844×390、390×844 三種尺寸驗證（DOM 或場景物件的可見性斷言＋截圖），無 console error／pageerror。

## 整體

- **A11** `npm test` 全綠、`npm run build` 成功；瀏覽器治具預設／`--assist`／`--motion` 與新增的平台模式都 PASS。
- **A12** fresh opus 對抗審查；CRITICAL／HIGH 修完並經修補覆審（最多 3 輪）。
- **A13** 部署：`npm run deploy:pages` 後 gh-pages 與發布工作成功；公開站 delivery 顯示 `direct-v4`。

## 修訂紀錄

- **2026-09-24 A7 修訂（使用者裁定，對抗審查 H2 選項 A）**：原文「原本的左右滑改朝向行為保留」改為「墊球時出手鈕上的滑動只送出平台選擇，不改朝向；扣球維持原行為」。原標準錯在：出手鈕滑 30 px 會轉身 42°，蓋過平台偏向，實測 15 次中 5 次把球送往反方向；這是 A3 用固定朝向所以量不到，直到對抗審查走真實觸控指令鏈才發現。另加 A3b：走真實觸控指令鏈，左右滑須改變橫向出球且不轉身。
- **2026-09-24 自動迎球修訂（使用者裁定）**：迎球轉身改為面向來球路線；為保留接偏球的容錯，加入平台側伸（最多 0.2 倍身高，速度受限，膠囊不變，同樣從表面速度排除）。
- **2026-09-24 新增 A14／A15（使用者同意凍結，對抗審查 M2 後加嚴）**：量測範圍為正式墊球餵球，站位 x ∈ {−0.2, −0.1, 0, 0.1, 0.2} × z ∈ {4.8, 5.0, 5.2}，按鍵時機第 18–40 tick，朝向正對球網，只計主動前臂或手觸球；結果以球第一次結束（ground／net／out）判定。
  - A14a 不滑碰網率 ≤ 10%；A14b 不滑落在舉球區（地面，z 0.5–3 m，|x| ≤ 3 m）≥ 50%；A14c 不滑觸球後弧頂中位數 ≥ 3.0 m；A14d 低球碰網率 ≤ 25%。
  - A15a 不滑命中中央目標（地面落點距 (0, 1.6) ≤ 1 m）≥ 40%；A15b 左滑命中左目標 (−2, 1.6) ≥ 25% 且 ≥ 3 倍於不滑命中左目標的比率；A15c 右滑對稱。
  - 修訂前實測：不滑碰網 71%、舉球區 0%、弧頂中位數 2.77 m、低球碰網 69%、各目標命中 0%。
- **2026-09-24 既有測試修改（使用者同意）**：v3「轉身不加衝量」遊戲層測試改為母體「觸球時仍在轉身或側伸」，對照組兩者皆設為已完成，判準為每一例差異 < 0.3 m/s（原為中位數 < 0.15；改為面向來球路線後轉身母體只剩 24 例、兩邊中位數皆為 0，失去鑑別力）。實測現行最大 0.068 m/s，拿掉排除的突變最大 1.246 m/s。
- **2026-09-24 第二輪覆審後（使用者裁定「照建議，之後再考慮納入上滑與下滑」）**：
  - 上滑／下滑暫停用：輸入層一律送 `NEUTRAL`；模擬核心保留 `HIGH`／`LOW` 契約供日後重新設計。原因：中性平台調成到位球後，上滑 0% 落在舉球區、56% 落到身後，下滑 79% 越網，和裁定 ② 的語意相反；出球總速度大致固定、弧頂約 4.5 m 封頂，無法在不重新設計的情況下同時滿足。A7 相應改為「上下滑為 NEUTRAL」。A3 的 HIGH／LOW 模擬層斷言保留。
  - 自動迎球的來球路線改用球的世界速度（原為相對速度，玩家橫移會被放大成側向出球）。
  - 更正上一筆紀錄：「現行最大 0.068、突變 1.246」是中性平台重調前量的；重調後實測現行最大 0.000、拿掉排除的突變最大 0.943 m/s（母體 140 例：轉身 16、只有側伸 124），判準 < 0.3 仍能區分。
  - 更正：上一輪把 A5 的錄影改成全程 LEFT 並稱為「補強」，實際上是縮小案例；已恢復原本「準備期中途 LEFT 換 HIGH」的錄影，與全程 LEFT 並存。
