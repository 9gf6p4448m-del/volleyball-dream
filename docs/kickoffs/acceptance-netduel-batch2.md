# 接球微回饋 批2 驗收(動手前凍結,2026-08-27)

拍板見 net-duel-juice-kickoff.md。基準=批1 落地後的 HEAD、`npm test` 全綠數。
範圍=sim/game.js(僅丙3 perfect 欄位外露)+matchLoop.js(丙1/丙2 timescale 觸發)+
ballView 或對應視覺檔(丙3 發光)+新測試。

- NJ-1 `npm test` 全綠(含批1 新增測試零紅)。
- NJ-2 丙1 hit-stop:重扣被我方救起(dig)的觸球瞬間→畫面定格(delta=0)一段
  【試玩必調】常數時長(提案 90ms)+輕微鏡頭震動;觸發限「重扣」——判定沿既有
  重扣定格同一門檻/來源,**不得另抄數字**;每次觸球至多觸發一次。
- NJ-3 丙2 魚躍慢動作:dive 姿態(divedUntil 路徑)期間完成的成功救起(球未落地、
  我方繼續)→接短慢動作(重用既有 delta×0.35 通道),時長【試玩必調】常數;
  魚躍撲空(球落地)不播——慢動作只獎勵成功。
- NJ-4 丙3 完美接球:sim 接球 TOUCH 事件補 `perfect:boolean` 唯讀欄位,判定=既有
  receivePerfectMul 同一來源(t≥0.95,不得另抄 0.95);表現層吃該欄位→球短暫
  發光+音效疊層。**sim 行為零漂移**:除事件多一欄位外,任何既有測試零紅、
  決定論測試零紅(同 seed 逐 tick 相同)。
- NJ-5 sim diff 最小化:`git diff --stat` 中 src/sim/ 僅 game.js 一檔,且 diff 內容
  僅事件欄位外露(無數值邏輯行變動);逐行可對應 NJ-4。
- NJ-6 突變實測 ≥2:①丙3 判定改抄死 0.95 字面(脫離 receivePerfectMul 來源)→
  單一來源斷言必紅;②丙1 觸發拿掉「重扣」限定→普通輕球救起也定格,行為斷言必紅。
  各 ≥1 紅可歸因,紀錄寫測試檔頭(真的做過才寫)。
