# 大作感卷 批4 驗收凍結（2026-08-28）— UI 皮膚「冠軍黑金 × 動感構圖」

> 出處：批 4 依 R6 品味題程序出圖讓 Sawmah 挑（共三輪：A–C → 混合案+D/E → E×A 組合），
> **2026-08-28 定案**：主選單＝方向 E 冠軍典藏（黑金、金框、置中、襯線金漸層標題）；
> 比賽畫面＝方向 A 的斜切動感構圖 × E 的黑金配色。參考稿（像素級依據，實作以此為準）：
> session scratchpad `juice-ui/dir5-champion.html`（選單）與 `juice-ui/ea-hybrid.html`（比賽 HUD），
> 快照另存 repo `docs/kickoffs/juice-ui-refs/`。批 3 光效（另批）色調沿用本案金色。
> 本檔凍結後不得為讓驗收變綠而改動（02 §2.1）。

## 視覺識別（單一事實來源，進 tokens 模組）

- 配色：底 `#0b0906`/`#14100a`（近黑）、金 `#d4af37`、亮金 `#f4e3ae`、暗金 `#9a7b1e`、
  米金文字 `#f0e2b6`、輔助 `#cbb886`。
- 字體：中文 `Noto Serif TC`（700/900）、拉丁與數字 `Cinzel`（600/700），Google Fonts 載入，
  `font-display: swap`；離線/載入失敗＝系統 serif fallback，**不阻塞不致死**。
- 構圖語彙：選單＝置中對稱＋細金框（雙線）＋金漸層標題；比賽 HUD＝斜切（skewX 約 -8~-12deg）
  色塊、實心投影、金色速度線點綴。

## 範圍（本批做）

1. **tokens 模組**：色彩/字體集中單一來源（新檔，如 `src/ui/theme.js`），字體 link 掛載；
   既有各檔寫死的顏色**只改本批觸及的畫面**，不做全 repo 大掃除。
2. **入口畫面＋生涯主選單**（careerScreen 的標題畫面與 renderHome 列）＝E 樣式：
   金框、金漸層標題、金框按鈕（主按鈕金底黑字）。存檔槽畫面（選擇你的夢）沿用同 tokens
   基本換色（底/框/字），不要求逐元素重排。
3. **比賽 HUD**＝A 構圖×黑金：比分板（斜切塊：隊名金塊/數字黑金塊/SET 中塊）、
   氣勢條（斜切金漸層）、字卡（pointBanner/toast：斜切黑匾金框）、得分事件卡、
   體力條（斜切金漸層）、離開/暫停/🎬 按鈕 chips、zonePanel 決策面板按鈕（同語彙換皮）。
4. **不做（後續另批）**：批 3 光效；careerScreen 深層子頁（名冊/續約/職業年表等）全面重排
   （只吃到 tokens 的底色字色就好）；netLobby 完整重skin；3D 場內元素（球員/看板）零改動。

## 驗收條件

- **A1**：tokens 單一來源存在，選單與比賽 HUD 的新樣式色值/字體都引用它（grep 不得在本批
  新寫的樣式裡出現寫死的重複色值——引用 tokens 常數或 CSS 變數）。
- **A2**：入口畫面與 renderHome 實際渲染＝E 樣式（實截圖比對參考稿：金框、金漸層標題、
  金框按鈕；由主對話 Playwright 實截驗收，容許非像素級的合理差異）。
- **A3**：比賽中實際渲染＝A 構圖黑金（實截圖：斜切比分板/黑匾字卡/金體力條/chips）。
- **A4**：字體離線 fallback：斷網（或 stub 字體載入失敗）下頁面正常渲染系統字、無 console
  error 阻塞、無 fatal。
- **A5**：sim 零改動：`git diff src/sim/` 空；sim-hash 開工前後逐場同。
- **A6**：全套測試綠（現 2300＋新增）。既有測試若斷言到被改的 DOM 樣式/文字，**不得改測試
  遷就**——先回報再議（02 §2.1）。
- **A7（試玩項）**：Sawmah 真機：可讀性（陽光下對比）、FPS 無感知下降、質感方向對。
  所有色值/斜切角度/字級標【試玩必調】。

## 風險誠實揭露

- 中文 webfont 體積大：走 Google Fonts unicode-range 分片漸進載入；離線＝系統字（可接受的
  降級，PWA 不預快取字體）。
- careerScreen 大量 inline style：只動指定畫面，避免整檔重構（Surgical Changes）。
