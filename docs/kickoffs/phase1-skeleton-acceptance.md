# Phase 1 骨架驗收清單（Sawmah 用）

> Claude Code 回報「骨架完成」後，用這份逐項驗。**全綠才放行 H 區手感層。**
> 任一項紅燈＝地基有裂縫，現在補最便宜；疊了手感再補要連手感一起重寫。
> 驗法欄告訴你「怎麼親手確認」，不要只信它說「做好了」。

---

## A. 架構鐵律（破一條就退回）

| # | 驗收項 | 怎麼親手確認 | ✅/❌ |
|---|--------|-------------|------|
| A1 | `src/sim/` 零 three.js/DOM | 在 `src/sim/` 全域搜 `three`、`window`、`document`、`canvas`——應零命中。或直接 `node` 跑 sim 測試，不 import three 也能過。 | |
| A2 | sim 能在純 node 跑完一局 | 跑「餵 Intent 序列驅動一局」的測試，不開瀏覽器、不接畫面，能跑到某隊 25 分。 | |
| A3 | render 不寫回 sim | 搜 render 層有沒有對 sim state 賦值（`simState.x =`）。應只讀不寫。 | |
| A4 | main.js 維持不鎖幀＋固定步長累積器 | 看 main.js 還是 rAF + accumulator 結構，沒被改成鎖 60。 | |

## B. 決定論（多人的命根，最容易偷偷破）

| # | 驗收項 | 怎麼親手確認 | ✅/❌ |
|---|--------|-------------|------|
| B1 | 同輸入同種子 → 同結果 | 跑決定論測試：同一組 Intent + 同種子執行兩次，最終比分＋事件流完全一致。**這是最關鍵一項。** | |
| B2 | sim 內無非決定論源 | 搜 `src/sim/`：`Math.random(`、`Date.now(`、`performance.now(`、`new Date`——應零命中（隨機須走種子 PRNG，種子在 state 裡）。 | |
| B3 | AI 決策也決定論 | B1 的測試裡 AI 有實際行動（不是空局）。11 個 AI 都動過，重跑仍一致。 | |
| B4 | 固定步長、判定與幀率脫鉤 | 確認判定邏輯吃 tick 不吃 rAF delta。（Phase 0 已落地，確認沒被 D 區改壞。） | |

## C. 資料層 v1（D1）

| # | 驗收項 | 怎麼親手確認 | ✅/❌ |
|---|--------|-------------|------|
| C1 | 屬性 8 項到齊 | Player.attributes 有 jump/power/reaction/stamina/speed/control/**serve**/**block**。 | |
| C2 | 身高即時推導、無衍生欄位 | 搜有沒有存「blockHeight」「reachHeight」之類欄位——應該沒有，全由 height.current 算。 | |
| C3 | trust 與 height.timeline 已進結構 | 結構裡有 `trust:{fromSetter}` 和 `height:{current,timeline}`，Phase 1 不驅動邏輯但欄位在。 | |
| C4 | naturalRole / currentRole 分離 | 兩個欄位都在、不是同一個。 | |
| C5 | 序列化 round-trip 測試綠 | 有 serialize/deserialize 純函式＋測試：還原後與原物件深度相等。 | |

## D. 比賽狀態機（D2）

| # | 驗收項 | 怎麼親手確認 | ✅/❌ |
|---|--------|-------------|------|
| D1 | 只吐事件、不管球員動作 | 看狀態機 output 是事件（得分/輪轉/死球因/觸球數），沒有球員移動座標。球員動作在 AI 層。 | |
| D2 | 四條規則正確 | 打幾局看：得分對、觸球超過 3 次判死球、換發時輪轉、後排球員前排攻擊被判違例。 | |
| D3 | deuce 正確 | 刻意打到 24–24，確認要領先 2 分才贏（24-24→ 26-24 才結束，不是先到 25）。 | |
| D4 | 站位違例有標 TODO | 搜 `TODO Phase 2` 有標到位置/輪轉站位違例，沒偷偷做也沒忘記標。 | |

## E. 回合 AI（D3・最難）

| # | 驗收項 | 怎麼親手確認 | ✅/❌ |
|---|--------|-------------|------|
| E1 | 雙層架構在 | 看得到「個體行為狀態機」與「球隊協調層」兩層，不是一坨。 | |
| E2 | **不互讓**（最醜破綻） | 跑「連續來球」測試：一堆球連續進場，**沒有任何一球落地時沒人動**。這項紅燈最傷沉浸感。 | |
| E3 | 不兩人同撲 | 同上測試：沒有兩個 AI 同時撲同一球打架。call 鎖定窗口有效。 | |
| E4 | 隊友/對手/補位共用同一套 | 確認我方 5、對方 6 走同一套 AI 碼，不是各寫一份。 | |
| E5 | 堪打不弱智 | 觀察一局：接發沒漏接、舉球沒亂傳給不存在的人。動作正確即可（不要求心理戰）。 | |

## F. Intent 管線（D4）

| # | 驗收項 | 怎麼親手確認 | ✅/❌ |
|---|--------|-------------|------|
| F1 | Intent 同型、玩家/AI 共用 | 玩家輸入與 AI 決策產生的都是同一個 Intent 形狀，走同一條管線進 sim。 | |
| F2 | playerId 對得上 Player.id | Intent.playerId 能對映到某個 Player.id，不是亂數/index。 | |
| F3 | tick 綁定 | 每個 Intent 帶生效 tick。 | |
| F4 | sim 不知來源 | sim 消費 Intent 時沒有 `if 玩家 / if AI` 的分支——它不該知道也不在乎來源。 | |

---

## 放行判準

- **A、B 區任一紅燈 → 不放行**，這是鐵律，補了再驗。
- **E2（互讓）紅燈 → 不放行**，這是排球 AI 的臉面。
- C/D/F 個別小項可視情況帶著 TODO 前進，但 C5、D3、F1 建議也綠再走。
- 全數確認後，回 Claude Code 開 H 區手感層，並準備真機 60 FPS 實測。

## 放行後第一件事
真機驗 **60 FPS 硬底線**（延續 Phase 0）＋ 跑一次完整決定論測試當迴歸基準——之後每疊一層手感都重跑這條，確保手感層沒污染 sim 決定論。
