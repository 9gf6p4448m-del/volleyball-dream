# 職業章批 4b 驗收凍結（2026-08-26）——改叫（案 B，拍板形狀 B2）

依據＝`pro-chapter-kickoff.md` 批 4 增補（B2＝直接下指令：盤點證實 AI S 組合是當下
抽籤無顯式叫牌可覆蓋，故重用 applyReplanCall 通道，把組合指令開放給非 S 位置）。

- **E1 解鎖**：TECH_DEFS 新增職業章技術（名稱提案「改叫」）；職業章傳授事件
  （一生一次、events 入帳、dialogPlay 契約＋字在對的容器）；**未解鎖零可見**。
- **E2 入口**：玩家非 S 位置、解鎖後，在既有叫戰術窗界內（同 canCallPlay 閘＋
  applyReplanCall 的窗）可直接下組合指令（交叉/時間差/B快——選項集合同 S 的⚡面板）；
  S 位置行為逐值不變；窗外不可叫（同既有窗界語意，不放寬）。
- **E3 代價**：改叫的那一波若**未得分**→fromSetter 信任扣**加倍**（相對既有失誤扣幅，
  倍數屬提案）；得分→照既有信任動態，不另給獎勵（地位是掙來的，不是按鈕給的）。
  代價走既有 trustDyn 管線，不另立第二套信任算法。
- **E4 sim 紀律**：允許的 sim 改動僅限指令通道（如 applyReplanCall 接受非 S 來源）
  且**未使用時逐值無效果**；sim-hash＝`34772c06e02243fd` 不動；AI 對 AI 治具不受擾。
- **E5 全套**：npm test 全綠（基準＝批 4a 收版）；改前紅照慣例（行為級；
  import 旁枝以壞版自證補）。
