# 攔網分工卷 — 開卷討論簡報

> 2026-07-31。基準＝`main@f54217b`（二速解封後的新工作點）。立案備忘＝`block-division-case-memo.md`。
> **本簡報照「開卷即關卷」打法**：先補量把選項空間釘死，裁定一次送。三路補量全部
> `src/` 零改動、40 局、決定論複跑逐值相同、`npm test` 849 全綠。
>
> ⚠ **本卷排在組合攻擊卷之前**（Sawmah 2026-07-31 裁定）——組合攻擊卷的驗收 6.2／6.4
> 全部預設「攔網手能被個別騙走」，在共用單一計畫的結構下不成立。

---

## 〇、三句話總結

1. **牆瞄不準是主病灶**，牆寬與人數都是次要的：守方 commit 的攻擊過網點離牆心 p50 **1.27–1.34m**，
   遠大於任何牆寬設定的半寬 ⇒ 加寬補不上（`BLOCK_SHOULDER_M` 掃四檔，八個 Δ **全部分不出**）。
2. **第三名攔網手的邊際貢獻是負的**：乾淨反事實下拿掉他，攻方得分率 **−5.46pp ± 1.77（顯著）**，
   而**被攔死率一格沒動**（−0.05 ± 0.61）⇒ 他在牆上完全沒有貢獻，增益全部來自「地板多一個人」。
3. **原本的裁定題 0（commit 該吃第幾個下降沿）前提錯誤**：46–83% 的球根本沒走到「沿」那條路，
   commit 實際起跳 p50 = **set+1**（走位準分支）。題目要重寫。

---

## 一、補量結果

### 1.1 A 路｜下降沿序列與 commit 起跳（`tools/block-edge-sequence-probe.mjs`）

**現行 commit 在哪裡起跳**（真實路徑，直接讀 `ai.blockPlan.jumpTick`）：

| 面對 | 起跳 p50 | **走位準**（觸球當下已 null，不是沿） | 走第 1 個沿 | 沒起跳 |
|---|---|---|---|---|
| 快攻 | **set+1** | **82.7%** | 9.9% | 7.4% |
| 兩翼三速 | **set+1** | **46.4%** | 32.6% | 21.0% |
| 兩翼二速（新） | **set+1** | **61.2%** | 24.8% | 14.0% |
| 後排 | **set+1** | **52.4%** | 37.0% | 10.6% |

「第 2 個沿」出現率 **0.0%**——位準一旦在 set+1 成立，後面的沿永遠輪不到。
獨立佐證：既有 `tools/phase5-block-jumpcount-probe.mjs` 對 commit 三組全為 set+0（差 1 tick 是 setTick 取樣點差異，逐值對得上）。

**每個沿「指著誰」**（探針特權：與實際擊球者 pid 比對；AI 拿不到此資訊）：

| 型態 | 第一個沿命中真攻擊手 | 最後一個沿命中 | 真攻擊手曾產生過自己的沿 |
|---|---|---|---|
| 快攻 | 15.1% | 13.7% | 63.4%（p50 set−5，早落在觸球前） |
| 兩翼三速 | 34.8% | 33.0% | 33.0% |
| 兩翼二速（新） | 20.3% | **9.8%** | 17.8% |
| **後排** | **0.0%** | **0.0%** | **0.0%（0/227）** |

- **後排是結構性天花板**：助跑起點 `lz 5.61` 恆 > `BLOCK_COMMIT.DEPTH_LZ = 2.9`
  （`src/sim/blockRead.js:126,197`）⇒ 後排攻擊手**永遠不可能**被 `blockCommitRead` 指到。
  「往後吃第幾個」不論調到多少，對後排恆為 0。
- **二速把「往後吃」的方向弄反了**：最後一個沿有 **49.6% 指向 quick（中路誘餌）**，
  比第一個沿的 32.3% 更常是誘餌 ⇒ 「吃最後一個」在二速上是**淨退步**。這是二速解封帶進來的新現象。
- **「最後一個」結構上不可即時判定**：`blockCommitRead` 不含「還會不會有人進偵測範圍」的資訊，
  實測沿間距 max 53 tick。用「等 W tick 沒有新沿」近似，快攻的窗涵蓋率從先知的 100% 掉到 **27.4%**。

**舊統計全部推翻**（`ai.js:1340-1342` 記載的 2 個沿 51%／3 個沿 15.7%／第一個 set+35／最後一個 set+82）
——兩種取樣口徑都不成立，非口徑差異造成。

### 1.2 B 路｜第三攔網手邊際貢獻（`tools/block-marginal-probe.mjs`）

**干預臂 cap2b**（乾淨的「單一攻擊點最多 2 人上牆、第三人回落」反事實），對守方 B（commit），40 局同 seed 配對：

| 指標 | base | cap2b | Δ ± SE | 裁決 |
|---|---|---|---|---|
| 攻方得分率 | 58.1% | 52.6% | **−5.46pp ± 1.77** | **顯著（3.1 SE）** |
| **被攔死率** | 4.7% | 4.7% | **−0.05pp ± 0.61** | **分不出** |
| 觸手改變球路率 | 13.9% | 12.3% | −1.60pp ± 1.32 | 分不出 |
| 救起率 | 32.6% | 39.0% | **+6.47pp ± 1.66** | **顯著** |

**讀法**：拿掉第三人，防守變好 5.5pp；被攔死率完全沒動 ⇒ **第三人在牆上零貢獻**，
所有增益來自他回到地板。對守方 A（read）則是 −0.98 ± 1.26＝分不出（A 本來只有 10% 的球三人跳）。

**兩個掛點的實測**：
- **`blockFarWing` 不是可用的槓桿**（cap2a 臂全指標分不出）：`lane` 只吃 `currentRole`
  （`ai.js:1652`：middle=0／outside=−1／其餘=+1），輪轉到前排兩人同 lane 號時**兩人一起退或一起留**，
  只能把 nJ=3 從 70.4% 壓到 47.8%。分工規則**需要新掛點**。
- ★**既有結構債**：`ai.js:1679` 的 `tier = Math.abs(i - main)`，主攔站三人中間時 tier 是 **1/0/1，
  根本不存在 tier 2** ⇒ `ai.js:1007` 那條「tier≥2 才退」的既有退路**對半數情形恆不成立**
  （實測 nJ=3 只從 70.4% 動到 71.7%）。**這是一個恆假的判斷式**，與二速 `takeoffLead` 同型的病。★

### 1.3 B 路｜吊球現況

| 攻方 | 類別 | 佔比 | 得分率 | 被救起 | 被攔死 |
|---|---|---|---|---|---|
| A | dump/tip | 12.5% | 22.9% | 63.4% | 6.9% |
| A | 一般強攻 | 87.5% | 63.3% | 28.0% | 4.2% |
| B | dump/tip | 11.8% | 35.5% | 48.8% | 1.7% |
| B | 一般強攻 | 88.2% | 67.8% | 22.0% | 2.9% |

吊球救不起來時**落點全在前區**（守方 B 100% 離網 <3m，離網 p50 2.41–2.45m、|橫向| p50 1.22–1.35m）。

**「第三人退防後站得到嗎」——三條路徑分開報**：
- **① 重建模型**（`ai.js:979-981` 現行退防目標 x=±3.0／z=±2.6）：離落點 p50 **4.33m**，≤2m 僅 **15.0%**
  ⇒ **現行的退防座標接不到吊球**。要做「第三人補吊球」必須另定退防點。
- ② 真實路徑（base，落點到未上牆前排最近距離）：p50 1.00m，100% ≤2m
- ③ 反事實 cap2b：p50 1.14m，95.5% ≤2m

現況誰在救吊球：守方 B **100% 由前排救起，其中 62.2% 是「剛剛在牆上跳過的人落地自己撿」**；
cap2b 下降到 27.6%（改由專職退防者接手）。

### 1.4 B 路｜牆寬 `BLOCK_SHOULDER_M` 敏感度

| 肩距 | 得分率(B) | 被攔死(B) | 三人牆覆蓋寬 p50 | 過網點離牆心 p50 | 得分率 Δ vs 0.55 |
|---|---|---|---|---|---|
| 0.40 | 57.3% | 4.7% | 2.09m | 1.32m | −0.56 ± 1.51 分不出 |
| **0.55（現行）** | 58.3% | 4.6% | 2.10m | 1.33m | — |
| 0.70 | 60.1% | 4.3% | 2.40m | 1.32m | +2.22 ± 1.40 分不出 |
| 0.85 | 57.7% | 4.5% | 2.70m | 1.34m | −0.19 ± 1.24 分不出 |

幾何確實照著動（覆蓋寬 2.09→2.70m、落在帶內 +8.1pp），但**八個 Δ 全部分不出**。
原因直接看得出來：**過網點離牆心 p50 就有 1.27–1.34m，遠大於任何一檔的半寬**——瞄準誤差主導，
加寬 0.3m 補不上。守方 A（read，瞄得準：離牆心 p50 0.49m、帶內 79.7%）同樣全部分不出。

### 1.5 C 路｜拆分工程面盤點

- **`blockPlan` 直接消費點：1 檔（`src/sim/ai.js`）、28 處**。間接消費：`game.js` 3 處（透過 `intent`／`actor` 派生）。
  `src/sim/blockRead.js`／`blockBand.js`／`src/input/blockRead.js`／`src/render/blockShadow.js` 逐字掃描**零命中**。
- ★**下游已經準備好**：`blockBand.js` 的 `members` 陣列**本來就是 per-blocker**
  （`game.js:1005-1021` 對每個前排球員各自 push `{id, x: actor.x}`）⇒ 幾何結算層完全支援三人站不同 x。
  **牆擠成一堆是決策層造成的，拆分下游一行都不用動。**★
- ★**既有先例**：`wallBail` 已經是 `playerId` 鍵值的字典（`ai.js:1011-1025`）；
  但同段落的 `chase`（`ai.js:1393,1398-1406`）是**單一共用值**，多名球員各自呼叫時後者覆寫前者
  ——兩種寫法不一致，看起來像漏改。★
- **欄位歸屬**：`team` 共用；`x`／`enterTick`／`jumpTick`／`jumpAt`／`replantUntil`／`pendingX`／
  `blind`／`seen`／`hand` 全部本該 per-blocker；`wallBail` 已是；`chase` 該是但現在不是。
- **題 4 的事實**：接發（`dutyPosition` `ai.js:668-687`）／防守（`digTargetFor` `:701-722`）／
  掩護（`coverPosition` `:728-742`）**全是逐人各自呼叫的純函式** ⇒ `blockPlan` 是系統裡的**例外**而非常態。

### 1.6 款 3 警報器在新工作點的現值

| 項目 | 值 |
|---|---|
| commit 快攻離地率 | 26.26%（52/198） |
| read 快攻離地率 | 20.73%（51/246） |
| **gap** | **+5.53pp**（門檻 5pp，**餘裕僅 0.53pp**） |
| 二速解封前 | +8.5pp（**被吃掉 2.97pp**） |
| 測試狀態 | 綠（849 全過） |

**P4 快攻列 G% 已翻轉**：read 49.1 > commit 40.3（margin **−8.8pp**；上次量測為 commit 45.2 > read 43.3，+1.9pp）。
但 `tests/block-persona.test.mjs:313-315` 已把 G% margin 判為「空載體、該指標已退役」，
款 3 的真載體改為離地率 gap（仍守住）。**兩者是否矛盾，本簡報不判定，列為裁定題 5。**

---

## 二、裁定題

> ## ✅ 裁定結果（Sawmah，2026-07-31）
>
> | 題 | 裁定 | 要點 |
> |---|---|---|
> | **0 起跳訊號** | **甲＝不動訊號，只做站位分工** | 風險最低、不碰憲法 §零、款 3 警報器最不容易被碰。「牆瞄不準」的主病灶本卷不解——依據＝§1.2 顯示光把第三人改去地板就有 5.5pp，本卷不依賴瞄準修好 |
> | **1 拆 per-blocker** | **拆** | 不拆則「個別騙得到攔網手」結構上不可表達，組合攻擊卷驗收 6.2／6.4 量不出效果 |
> | **2 分工規則** | **最多 2 人上牆＋重定退防點** | 照 cap2b 實測落地；`blockFarWing` 不能當掛點（被 `currentRole→lane` 卡死），需新掛點；退防座標另定（現行離吊球落點 4.33m） |
> | **3 牆寬** | **本卷不動** | 八個 Δ 全部分不出；要它有意義得先讓牆瞄得準（題 0 甲＝本卷不解） |
> | **4 是否升格系列** | **不升格** | 接發／防守／掩護全是逐人各自呼叫純函式，`blockPlan` 是例外 ⇒ 攔網決策層單點修復 |
> | **5 款 3 警報器** | **乙＝維持門檻，翻了停手回報** | 不改門檻以免變成「改門檻遷就實作」；預授權＝翻了就停手送裁（與 §十-4 那卷一致，該次真的翻了、停手後才找出 A 案） |
>
> **落地順序（分兩個 commit 以利 Δ 歸因）**：
> 1. **拆 per-blocker——純重構，行為必須中性**。機械驗收＝`tools/sim-hash-probe.mjs` 與拆分前**逐值相同**
>    （注意：基準檔本身已過期，比較對象是「拆分前的當前 HEAD」不是基準檔）
> 2. **分工規則＋重定退防點**——唯一的行為改動，Δ 全部歸因於此
>
> **`ai.js:1679` 的 `tier` 恆假結構債**：隨題 2 的新掛點一併處理（留著一個在半數情形恆不成立的
> 判斷式，與本專案剛修掉的二速 `takeoffLead` 同型）。
>
> ### ✅ 追加裁定：step 2a 平衡熔斷（Sawmah，2026-07-31）
>
> step 2a 落地後配對熔斷觸發（基準現做於 `3b684a2`，40 條生涯）：
>
> ```
> Δgroup-1  +27.5±8.8   Δgroup-2  +35.0±9.8   Δgroup-3  +5.0±9.4
> Δqf        -5.0±11.8  Δsf        -7.5±12.1  Δfinal   -17.5±10.7
> Δ奪冠率    -7.5±4.2
> ```
> 主判 Δ奪冠 ±15pp **過**；加掛主判 Δ決賽 ±15pp **不過**；輔助熔斷「任一輪 |Δ|>25pp」**觸發兩次**。
>
> **機理（解讀，非實測結論）**：Δ 沿對手強度單調遞減（+35 → +27.5 → +5 → −5 → −7.5 → −17.5）
> ＝**變異數下降**——兩隊防守同時變強 ⇒ rally 變長 ⇒ 隨機性減少 ⇒ 強者恆強。
> 早輪玩家是強者故贏更多，決賽玩家是弱者故輸更多。**不是某一隊的 bug，是系統性位移。**
>
> **裁定＝甲：接受，難度另卷重算。** 依據：補量顯示第三名攔網手在牆上零貢獻、在地板有用
> ——這是**模型正確性**問題，不是難度旋鈕。先把模型改對、難度當調校層另卷處理，是正確的先後。
> 先例＝§十-4 已接受「難度曲線會被重算兩次是接受的代價」。
> **代價（已知並接受）**：難度重校卷落地前，早輪會有一段時間過於容易（group-2 勝率 +35pp）。
>
> ⇒ **新增待辦：難度重校卷**（原本就有一項「全面 commit 派任」等在這裡，
> 見 `phase5-section10-4-stage2` 裁定丙——兩者應合為同一卷）。



### 題 0（**重寫**）｜commit 的起跳訊號要不要重做

**原題「commit 該吃第幾個下降沿」的前提被實測推翻**：46–83% 的球根本沒走到沿，
commit 在 set+1 就靠位準拔起。且往後吃只會更糟（第一個沿命中 24.6% → 最後一個 22.0%，
二速從 20.3% 掉到 9.8%，後排恆 0%）。

可選方向：
- **甲**：不動起跳訊號，本卷只做站位分工（風險最低，但「牆瞄不準」的主病灶不解）
- **乙**：處理位準分支（讓 commit 不再在 set+1 無條件拔起）——這是真正的病灶所在，但直接動款 3 警報器
- **丙**：改候選定義／`DEPTH_LZ`（`blockRead.js:126,197`）讓後排與二速攻擊手進得了偵測範圍
  ——解開後排恆 0% 的天花板，但那是「commit 看得到什麼」的定義，屬憲法 §零 領域

⚠ 注意 `ai.js:1339` 明載「該吃第幾個下降沿實質上就是 read／commit 賭局的定義本身（憲法 §零 領域），
不由實作端自行決定」。**這一題必須由 Sawmah 裁。**

### 題 1｜`blockPlan` 拆成 per-blocker？

工程量已知：**1 檔 28 處**，下游零改動，`wallBail` 有既有先例。
不拆的話，「個別騙得到攔網手」在結構上不可表達，組合攻擊卷的驗收 6.2／6.4 量不出效果。

### 題 2｜分工規則的形狀

數據支持「單一攻擊點最多 2 人上牆、第三人回落」（攻方得分率 −5.46 ± 1.77 顯著、被攔死率零變動）。
但有兩個附帶條件：
- **`blockFarWing` 不能當掛點**（cap2a 全指標分不出，被 `currentRole→lane` 的表達力卡死）——需要新掛點
- **退防座標要另定**：現行 `ai.js:979-981` 的 x=±3.0／z=±2.6 離吊球落點 p50 4.33m、≤2m 僅 15%
- 另需裁：`ai.js:1679` `tier` 恆不存在 2 這個結構債要不要一併修

### 題 3｜`BLOCK_SHOULDER_M` 動不動？

**數據說不用動**（八個 Δ 全部分不出）。它要變成有意義的旋鈕，前提是先讓牆瞄得準（題 0）。
建議列為「本卷不動」，但需 Sawmah 確認。

### 題 4｜是否為「防守端個體化」系列的第一案？

**事實傾向「不是」**：接發／防守／掩護全是逐人各自呼叫的純函式，`blockPlan` 是例外。
⇒ 本案可視為攔網決策層的單點修復，不必升格為系列。

### 題 5（**新增**）｜款 3 警報器怎麼辦

餘裕只剩 **0.53pp**，而本卷要動的正是攔網時序 ⇒ **一碰就翻**。可選：
- 甲：本卷開工前先重新校準門檻（需說明校準依據，避免變成「改門檻遷就實作」）
- 乙：維持門檻，把「翻了就停手回報」寫進本卷的預授權
- 丙：連同 P4 的 G% 一起重新檢視這組警報器的載體是否還有效

---

## 三、驗收草案（待裁定後定稿）

- [ ] 三人牆：`nJ=3` 比例由 commit 隊的 **70.4%** 降至門檻以下（門檻待定）
- [ ] 牆心離球過網點：commit 隊兩翼由 **1.66m** 降至 read 隊水準（0.79–0.83m）附近
- [ ] 攻方得分率變化在配對熔斷帶內（`tools/balance-sim.mjs` ＋ **卷基準現做**，
      ⚠ 不得用 `balance-paired-baseline-commitflag.json`，該檔已過期）
- [ ] 款 3 警報器依題 5 的裁定處置
- [ ] 決定論：同 seed 逐值重演
- [ ] `npm test` 全綠；sim-hash 基準重寫（**含清償 `b58ad3d` 未重寫即部署的既有債**）
- [ ] 手機 60 FPS
- [ ] gh-pages 送達證明

---

## 四、工具遺產

- `tools/block-edge-sequence-probe.mjs`——下降沿序列（兩種取樣口徑）、沿指著誰、窗涵蓋率
- `tools/block-marginal-probe.mjs`——邊際貢獻（觀察臂＋干預臂 cap2a/cap2b）、吊球、牆寬掃描（7 臂）
- `tools/triple-block-probe.mjs`——攔網人數分佈／集中度／按型態拆，四臂對照

## 五、本卷不做清單

- 組合攻擊卷的任何內容（本卷是它的前置）
- `blockUntil` 48 tick 演出殘影（`matchView.js:262`）——登記在備忘 §七，可獨立處理
- 全面 commit 派任（裁定丙，條件＝未來難度重校卷）
- tool 瞄手重開（十-4b 裁定乙，條件＝未來攔網時序卷）
