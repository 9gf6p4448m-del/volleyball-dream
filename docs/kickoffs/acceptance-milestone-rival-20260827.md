# 驗收凍結 — 里程碑事件＋宿敵生涯鏡像（小批試水，2026-08-27 夜）

> 拍板（Sawmah 本日）：B1 形狀＝乙（敘事＋小獎勵）；B2 方向＝甲（王勝翔生涯鏡像）；
> 量＝小批試水。落點基準：main `cdc860a`、2236 全綠。
> 範圍鐵律：`src/sim/` 零改動；全落 career/ui 層；所有數值與文案屬提案（試玩即改）。
> 設計裁定（主對話，防敘事與畫面打架）：鏡像**不含轉隊/出海**——名冊與對陣畫面裡
> 王勝翔仍在蒼羽泰坦，敘事不得與可見事實矛盾；動態限「不動隊籍的可觀察事實」。

## M 里程碑事件（三個，職業章）

- M1 三個事件與觸發（提案值，常數單一來源）：
  ①「老兵之年」＝職業年資達第 5 年；②「王朝」＝職業冠軍累計達 3 座；
  ③「最後衝刺」＝職業年資達第 9 年。觸發判定只吃**既有封存資料**
  （archive 的 pro 季數／proFinish==='champion' 計數），不新增記帳欄位。
  **變紅**：任一事件觸發判定讀不存在的欄位（舊檔炸掉）；或用了新記帳欄位。
- M2 演出＝敘事卡（dialogPlay line 一律 {speaker,text} 物件——探針卷空白泡泡教訓）
  ＋小獎勵一項（提案：全隊信任 +2 級別；常數具名、career 層、不進 sim）。
  **變紅**：line 用字串（wiring 測試斷言字在對的容器）；獎勵無具名常數。
- M3 終身各一次：旗標入存檔；同條件次年不重複；舊檔缺旗標欄位＝視為未觸發、不炸。
  **變紅**：連續兩年推進觸發兩次（測試斷言恰一次）；舊檔載入 throw。
- M4 條件未達不觸發：年資 4／冠軍 2 的存檔推進一年，三事件全不出。
  **變紅**：任一事件在未達條件時出卡。
- M5 測試：M1–M4 逐條有測試，每道守衛做刪除突變恰紅一條（前置閘≥1 紅可歸因）；
  `npm test` 全綠；sim-hash 不動（G：diff 不含 src/sim/）。

## R 宿敵生涯鏡像（王勝翔年表＋重逢句變體＋退休收束）

- R1 決定論年表＝career 層常數表（提案）：第 3 年隊長、第 5 年 MVP、第 7 年低谷、
  第 9 年宣布最後一年、第 10 年退休。不吃隨機、不吃比賽結果、不動隊籍。
  **變紅**：年表讀 Math.random 或比賽結果；或含轉隊/出海敘事。
- R2 年度重逢輕量句依年表換句（≥4 種變體＋既有預設句）；玩家出海期間換隔海變體句
  （既有 foreign 判定重用，不新增判定）。**變紅**：對應年份出的不是對應變體
  （逐年斷言）；出海年仍出國內句。
- R3 第 10 年退休收束卡：終身一次、他退休後重逢句不再出。既有挖角大事件
  （終身一次）與同隊/敵隊互斥**零改動**（既有 rival 測試零漂移）。
  **變紅**：收束卡出兩次；退休後仍出重逢句；既有 proEvents/rival 測試任何一條要改
  預期值（改了＝漂移）。
- R4 玩家與王勝翔**同隊**期間的年表事件句用同隊語氣變體（至少隊長/MVP 兩款），
  不得出「隔網相對」語意。**變紅**：同隊存檔出敵隊句（測試斷言）。
- R5 測試：R1–R4 逐條有測試＋刪除突變恰紅；全套綠。

## 全域

- G1 `npm test` 全綠且新增 ≥10 條；G2 diff 不含 `src/sim/`；G3 既有測試零改動
  （若治具介面必須補全，逐檔列明並不得動斷言）。
