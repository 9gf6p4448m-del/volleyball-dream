# 職業章批 4a 驗收凍結（2026-08-26）——餵線（案 A）＋賽前布置面板

依據＝`pro-chapter-kickoff.md` 批 4＋`pro-batch4-design-draft.md`（拍板：三案全做＋
面板照草稿；本批＝A＋面板，B2 改叫＝批 4b，C 二段時間差＝批 4c 地基級另派）。

- **D1 餵線解鎖**：TECH_DEFS 新增職業章技術（名稱提案「餵線」）；傳授走既有職業章
  事件慣例（一生一次、events 入帳）；**未解鎖＝零可見**（對陣卡/面板無任何新元素）。
- **D2 可視化**：解鎖後對陣卡顯示「對手眼中的你」——玩家全生涯扣打分佈
  （**讀 leagueScoutZones 真實聚合，不得另抄一份算法**）＋標記對手將押的線
  （scoutFocusZone 同一顆）；對手 scoutRead=0 或樣本不足＝誠實顯示「讀不到你」級文案，
  不畫假分佈。
- **D3 sim 零新判定**：反打得利走既有 scoutBlockMul 反常線折扣——本批不新增 sim 判定；
  `git diff --stat` 的 src/sim/ 若有改動僅限 config 參數通道且**預設值下逐值無效果**；
  sim-hash＝`34772c06e02243fd` 不動。
- **D4 布置面板**：職業章對陣卡兩布置槽——①攔網重心（對對方 ace 押慣用線側重，
  資料源＝career.scouting 該隊 zones）②發球攻擊（指定我方 AI 追打對方接發最弱者）。
  選擇寫 career 層、經 careerMatchSetup 參數通道餵我方 AI 傾向；**押錯有真實代價**
  （押線攔網＝方向錯牆站錯邊；追發＝失誤率上升——非純增益，數值屬提案）。
  佈置僅職業章可見（大學/企業/高中零改動）。
- ★2026-08-27 多年卷批 4B 輪 2 追記：下條「顯示『情報不足』」的**字面**已隨提案
  文案改為默契/接發情報語意（文案屬提案）；凍結的行為半（資料不足＝不可選、不給
  假資料）不變。詳見 acceptance-multiyear-batch4b.md 覆審處置紀錄。★
- **D5 情報不足態**：該槽資料不足（無 scouting 紀錄/樣本過少）＝顯示「情報不足」
  且不可選，不給空白或假資料。
- **D6 全套**：npm test 全綠（基準 1879＋新增）；sim-hash 不動；改前紅照慣例
  （行為級；import 旁枝以壞版自證補）。
